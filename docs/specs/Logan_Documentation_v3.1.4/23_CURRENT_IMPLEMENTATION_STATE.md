# Logan Intelligence — Current Implementation State
**Version:** 3.1.4
*Last updated: 2026-08-23 (Sprint 3.6.8 Block 5, ADR-060) — verified against the actual repository, not reconstructed.*

> This document was rewritten from a direct repository inspection (`git remote`, `git log`, `logan_core/` and `mobile/app/` directory listings) during the v3.1.3 reconciliation session. It supersedes the prior "UNVERIFIED IMPLEMENTATION SNAPSHOT" placeholder below, which predates this repository's actual `logan_core/` build (see `docs/DECISIONS.md` ADR-014 through ADR-028) and was never updated to match it.

---

## Repository

| Item | Value |
|------|-------|
| Remote | `https://github.com/lgarris09/logan-market-app.git` |
| Repo structure | Monorepo: `logan_core/` (canonical pipeline, ADR-014/017), `backend/app/` (historical FastAPI/SQLite prototype, still running, not extended — ADR-014), `mobile/` (Expo/React Native) |
| Primary language | Python (`logan_core/`, `backend/`), TypeScript/React Native (`mobile/`) |

---

## Mobile App (`mobile/app/`)

| Item | State | Notes |
|------|-------|-------|
| Expo Router screens | EXISTS | `index.tsx` (home/Attention Field), `field-legacy.tsx` (preserved radial Opportunity Field, ADR-027), `atmosphere-preview.tsx` (Skia atmosphere Sprint-1 preview, ADR-028, unchanged), `ask.tsx`, `classic.tsx`, `demo.tsx`, `memory.tsx` |
| Attention Field (depth-of-focus, `Vessel` component) | BUILT | Wired to real `/v1/opportunities` data (V3.1.4 BATCH-4); Atmosphere is now integrated as a subordinate background layer behind the Vessels (`AttentionAtmosphere.tsx`, V3.1.4 BATCH-5, resolves ADR-028's open item) — a narrower, capped version of the Sprint 1 preview, not the full particle/demo-entity version |
| Opportunity Field (legacy radial layout) | BUILT | Preserved unchanged, reachable via menu (ADR-023, ADR-027); still calls `/v1/demo/feed` (deprecated but functional) |
| API connection | REAL (primary screen), DEMO (legacy screens) | Home screen calls the real, versioned `GET /v1/opportunities` (V3.1.4 BATCH-4, `backend/app/opportunities.py`); `field-legacy.tsx`/`demo.tsx`/`classic.tsx` remain on their existing demo/legacy routes, all still functional (`deprecated=True`, not removed) |
| WebSocket client | NOT BUILT | |
| Opportunity Card (full spec, `22_OPPORTUNITY_CARD_SPEC.md`) | NOT BUILT AS SPECIFIED, but consolidated | Two near-duplicate card components merged into one canonical `OpportunityCard` (`DeliveredItem`-based, V3.1.4 BATCH-4); still not the full `22_OPPORTUNITY_CARD_SPEC.md` contract |
| Reduced motion | BUILT | `hooks/useReducedMotion.ts` (AccessibilityInfo-based) wired into AttentionField/Vessel/LoganCore/OpportunityNode; Atmosphere layers use Reanimated's own `useReducedMotion()` (V3.1.4 BATCH-5) |
| Accessibility (labels/roles/hints/states) | BUILT (partial) | Applied to every interactive element reached in V3.1.4 BATCH-5 (Vessel, home screen menu, OpportunityNode, ask/memory/demo/classic screens); not a full accessibility audit of every screen |
| Centralized fetch / loading-empty-timeout-retry-error states | BUILT | `lib/apiClient.ts` (timeout, bounded retry, `AbortSignal` cancellation); wired into the home screen only (V3.1.4 BATCH-5) |
| Mobile test suite (Jest/RNTL) | BUILT | First ever for this repo — 19 tests (`jest-expo` preset, `@testing-library/react-native@13`); wired into CI (V3.1.4 BATCH-5) |
| Design tokens (`spacing`/`radius`/`type`) | PARTIAL | Adopted on the flagship screen (`index.tsx`) and `Vessel.tsx` wherever an exact-value match existed; not a full sweep of every screen (V3.1.4 BATCH-5) |

---

## Backend

### `logan_core/` — canonical pipeline (ADR-014)

All 18 layer folders plus `contracts/`, `orchestrator/`, and `tests/` exist with real implementation files (not stubs) as of this session:
`contracts, orchestrator, receptors, normalization, world_model, evidence_trust, community_intelligence, memory, user_model, active_context, reasoning, mental_model, conclusion_confidence, opportunity, policy, prioritization, presentation, feedback, learning`.

| Item | State | Notes |
|------|-------|-------|
| Data contracts (Pydantic) | BUILT | `logan_core/contracts/` — `Domain` literal currently has 6 values (`stocks, sports, poly, social, news, crypto`); `culture`/`personal_finance` are documentation-only, not yet in code (see ADR-037 consequences; adding them is out of scope for V3.1.4 — broad domain expansion excluded) |
| `MemoryRecord.user_id` isolation (ADR-033) | BUILT | Required, non-empty, validated; threaded through `LearningEngine.process_feedback()` and the Orchestrator's feedback/Memory Inbox methods. `LOCAL_FOUNDER_USER_ID = "demo_user"` in `contracts/common.py` |
| `ActiveContext.user_id` (V3.1.4 BATCH-2) | BUILT | Required, non-empty, validated; `ActiveContextBuilder.build()` takes `user_id` as first positional param; threaded through `orchestrator/pipeline.py`'s `run()` |
| Feedback interaction types `watch`/`remind` | BUILT | `FeedbackEngine.interpret()` maps both deterministically; no ML behavior |
| ML model-version metadata (ADR-032) | RESERVED, NOT FUNCTIONAL | `EvidenceTrust.source_reliability_model_version`, `ConclusionConfidence.confidence_model_version`/`calibrated_at` — default `"deterministic-baseline"`; no trained model exists; scoring unchanged |
| `OutcomeRecord` v2 (ADR-036) | BUILT, UNWIRED | `schema_version "2.0"`; nothing in `logan_core/` constructs it yet — no scheduler processes outcomes (see `UNRESOLVED_QUESTIONS.md` #4) |
| `SourceObservation` contract | BUILT, UNWIRED | Future-facing sibling of `OutcomeRecord` for ADR-032 source-reliability calibration; not read by `EvidenceTrustEngine`, cannot affect current trust scores |
| `LearningEngine.process_outcome()` | STUB ONLY | Raises `NotImplementedError` — typed interface, no scheduler, no model training, no fake results (ADR-036) |
| `MentalModel.confidence` isolation (ADR-015, V3.1.4 BATCH-1) | FIXED | `ConclusionConfidence.confidence_score` derives from `EvidenceTrust.trust_score` only (plus contradiction penalty); Mental Model no longer blended in |
| `community_momentum` excluded from ranking (ADR-034, V3.1.4 BATCH-1) | FIXED | `internal_rank_score` (renamed from `priority_score`, ADR-029) no longer contains a `community_momentum` term; `global_importance = confidence.confidence_score` only |
| `internal_rank_score` public exposure (ADR-029, V3.1.4 BATCH-1) | FIXED | Renamed from `priority_score` and documented internal-only; `backend/app/logan_feed.py` now exposes ordinal `FeedItem.rank` (1-indexed) instead of any score; mobile consumers migrated to `rank` |
| Prioritization fatigue/cooldown (V3.1.4 BATCH-2) | BUILT | `FATIGUE_WINDOW`/`FATIGUE_LIMIT`/`COOLDOWN_WINDOW` wired into real expiration logic in `prioritization/engine.py`, previously unwired constants |
| `decision_trace` population | BUILT | Populated across all layers that produce a decision (normalization, world_model, evidence_trust, reasoning, conclusion_confidence, policy, prioritization, presentation, feedback, mental_model, community_intelligence); the `contradicting` field on world-model events and the `contradicts` branch in reasoning remain documented-unreachable — no deterministic contradiction rule exists without inventing per-domain semantics (out of scope) |
| Domain Receptors | SIMULATED by default; real, live-verified providers exist for stocks earnings/price/analyst-grade data but remain config-gated into the live backend path for NVDA earnings only (Sprint 3.6.6C) | `logan_core/receptors/simulated.py` — every domain's live backend path (`backend/app/logan_feed.py`) is simulated by default, NVDA included. `receptors/providers/`: `EarningsProvider`/`QuoteProvider`/`AnalystGradesProvider` Protocols, `FixtureEarningsProvider`/`FixtureMarketDataProvider` (explicitly labeled non-live, used by tests), `FmpEarningsProvider` (Sprint 3.6.6B) and `FmpMarketDataProvider` (Sprint 3.6.7 — `fetch_quote`/`fetch_latest_grade_change`), all live-verified against the real FMP API. `receptors/stocks_earnings.py` and `receptors/stocks_market_data.py` (Sprint 3.6.7) map provider data into `RawSignal`. As of Sprint 3.6.6C, `backend/app/logan_feed.py` calls `FmpEarningsProvider` for NVDA specifically, but only when `STRATUS_LIVE_NVDA_EARNINGS=true` is set (default: unset/off) — see the NVDA live-path row below. `FmpMarketDataProvider` is live-verified (`logan_core/live_verification/nvda_market_data.py`) and pipeline-integration-tested but **not yet wired into `backend/app/logan_feed.py`/`/v1/opportunities`** — deliberately deferred to Sprint 3.6.7 Block 2 (signal convergence), since `WorldModel.process()`'s `(entity_id, signal_type)` dedup key would silently drop one of two simultaneous live signal types for the same entity today. Every other entity, and NVDA itself when the earnings flag is off, remains simulated-only. See docs/DECISIONS.md ADR-042/ADR-043/ADR-044/ADR-051 |
| Orchestrator | BUILT | Owns Operational History writes (ADR-016); Sprint 3.6.6 added an opt-in `trigger_detection` step (`PipelineDependencies.trigger_detector`, defaults `None`) between normalization and World Model. As of Sprint 3.6.6C, `backend/app/logan_feed.py`'s shared Orchestrator instance wires this detector only when `STRATUS_LIVE_NVDA_EARNINGS=true` at construction time — with the flag off (the default), the trace is byte-for-byte identical to every pre-Sprint-3.6.6 caller (no `trigger_detection` layer entry at all, for any entity). `run_feedback_loop()` gained a content-builder callable option (ADR-049, Sprint 3.6.6 close-out) — existing string-content callers unaffected |
| World Model, Evidence Trust, Community Intelligence, Reasoning, Mental Model, Conclusion Confidence, Opportunity Engine, Policy, Prioritization, Presentation, Memory, User Model, Active Context, Feedback, Learning | BUILT | Each has a real implementation file (`engine.py` or equivalent); every layer now has direct unit test coverage plus a full pipeline integration test (`test_pipeline_tesla.py`) — **248 `logan_core` tests as of Sprint 3.6.7 Block 1 (2026-08-21)**, up from 197 as of the Sprint 3.6.6 close-out (see the section below for what the +51 cover: ADR-050's Personal-route authority rule and ADR-051's generalized stock-signal architecture). World Model (`trigger_events` on `EnrichedEvent`) and Evidence Trust/Conclusion Confidence (`trigger_confidence_bonus`, additive/default-zero) gained small, backward-compatible extensions in Sprint 3.6.6 — see ADR-042; Evidence Trust's `trigger_confidence_bonus` computation changed again in Sprint 3.6.6D (strongest-trigger resolution via `ConvergenceDetector`, not a sum — see below); Reasoning/Opportunity/User Model/Policy gained the ADR-047/048/049 behavioral-learning and Watch-route extensions (source-aware `connected_entities_explicit`/`connected_entities_inferred`, `_fold_behavioral_evidence`, `PolicyEngine`'s Personal/Exceptional routes); Prioritization's visibility/interruption decoupling (ADR-050) and `StocksTriggerEvaluator`'s multi-signal-type dispatch (ADR-051) are the two Sprint 3.6.7 additions — all additive, every pre-existing test still passes unmodified |
| Python tooling (Black/Ruff/mypy) | BUILT | Root `pyproject.toml`; dev-only deps in `logan_core/requirements.txt` and `backend/requirements.txt`; zero suppressions except 3 documented mypy limitations on default-arg-capture lambdas |
| `backend` test suite | BUILT | **82 tests as of Sprint 3.6.6 close-out (2026-08-21)**, up from 31 as of Sprint 3.6.6C (18 pre-3.6.6C + 13 for the config-gated live NVDA path; the remaining growth is Sprint 3.6.6E–I's notification/badge/engagement-timing work plus ADR-047/048's `/v1/interactions` and `UserModel`-persistence tests — see the Sprint 3.6.6 close-out section below) |
| NVDA live-path config gate (Sprint 3.6.6C) | BUILT, DEFAULT OFF | `backend/app/config.py`: `live_nvda_earnings_enabled()` reads `STRATUS_LIVE_NVDA_EARNINGS` (unset/false by default) and `load_dotenv()`s `backend/.env` for `FMP_API_KEY` at import time. `backend/app/logan_feed.py`: when enabled, fetches the latest NVDA report via `FmpEarningsProvider`, pre-checks it against the same `evaluate_earnings_beat_condition()` `StocksTriggerEvaluator` uses, and substitutes it for the simulated NVDA fixture (never both — dict-keyed replacement) only when the beat condition actually fires. Falls back to the simulated NVDA fixture — never a crash, never simulated data mislabeled live — on: provider construction failure (no API key), fetch failure (network/rate-limit/malformed), no report on file, or a real report that does not satisfy `STOCK_EARNINGS_BEAT`. `internal_rank_score`, the API key, and raw provider payloads are never exposed in the `FeedItem`/`/v1/opportunities` response contract, which is otherwise unchanged. Live-verified locally against the real FMP API 2026-08-14/15 and again 2026-08-18 (post-3.6.6D refactor, unchanged result): real EPS 1.87 vs. consensus 1.76 (beat_pct 6.25%), `confidence_score` 0.595. See ADR-044 |
| Mobile tooling (ESLint/Prettier/Jest) | BUILT | `mobile/eslint.config.js` (flat config, `eslint-config-expo`), `.prettierrc.json`; React-Compiler-readiness rules disabled with documented rationale. `jest-expo` + `@testing-library/react-native@13` (V3.1.4 BATCH-5) — 19 tests, this repository's first mobile test suite |
| CI (GitHub Actions) | BUILT | `.github/workflows/ci.yml` — 3 jobs: `logan_core`, `backend`, `mobile` (mobile's `jest` step runs for real as of V3.1.4 BATCH-5, previously a no-op placeholder) |
| TriggerEvent registry | PARTIALLY BUILT (Sprint 3.6.6, extended 3.6.6D and 3.6.7) — five stocks trigger codes, minimal contract | `logan_core/contracts/trigger.py` (a minimal `TriggerEvent`, not the full ~60-field `TRIGGER_EVENT_FRAMEWORK.md` contract — no revision/dedup model, `domain_impacts`, `lifecycle_effect`, `seasonal_context`, `causal_relationship`, `provider_disagreement_state`, `notification_eligibility`, etc.) and `logan_core/trigger_detection/stocks.py` implement `STOCK_EARNINGS_BEAT`/`MISS`/`IN_LINE` (sharing `actual_eps`/`consensus_eps`, mutually exclusive by fire-condition construction) plus, as of Sprint 3.6.7, `STOCK_PRICE_MOVE_SIGNIFICANT` (from a live `/quote` feed) and `STOCK_ANALYST_UPGRADE`/`DOWNGRADE` (from a live `/grades` feed, trusting the provider's own upgrade/downgrade classification) — all five from `TRIGGER_REGISTRY_STOCKS.md`. `StocksTriggerEvaluator.evaluate()` dispatches by `NormalizedSignal.signal_type` to a per-signal-type evaluator method; a sixth code plugs in the same way. Every other stocks code (`STOCK_EARNINGS_QUALITY_WARNING`, `STOCK_GUIDANCE_RAISED`/`LOWERED`, `STOCK_OPTIONS_FLOW_SURGE`, `STOCK_CONVERGENCE_MULTI_SOURCE`, etc. — each needing data the current FMP integration doesn't fetch, or a World Model dedup/merge change not yet made — see ADR-051) and every other domain's registry remain SPECIFIED — NOT IMPLEMENTED, unchanged from OD-009 (V3.1.4 BATCH-3). See docs/DECISIONS.md ADR-042/ADR-045/ADR-051, which partially — not fully — supersede OD-009. **Known duplication, not yet fixed:** every threshold and `confidence_contribution` value is a typed Python constant in `trigger_detection/stocks.py`, hand-copied from `TRIGGER_REGISTRY_STOCKS.md`'s markdown table with a comment citing it — there is no machine-readable registry loader, so the doc and the code are two independently-edited copies of the same numbers with no automated drift check between them. Verified consistent as of Sprint 3.6.7 (2026-08-21). Acceptable at 5 trigger codes; convert to a single machine-readable source of truth before trigger volume grows much further — explicitly deferred, not forgotten, per owner decision |
| ConvergenceDetector (Sprint 3.6.6D, narrow foundation) | BUILT, narrower than the canonical spec component | New `logan_core/convergence/` package: `ConvergenceDetector.resolve(trigger_events)` picks the single strongest `TriggerEvent.confidence_contribution` among everything attached to one `EnrichedEvent` (owner decision: never summed, never dropped) — wired into `EvidenceTrustEngine`, replacing its prior `sum(...)` computation. With today's mutually-exclusive earnings triggers, `max`/`sum` are numerically identical for every real case, so this changes nothing observable yet. **Not** the canonical Convergence Detector from `04_HIT_DETECTION.md`/`STOCK_CONVERGENCE_MULTI_SOURCE` (a Hit-Detection-layer, cross-domain/multi-source component with its own `OpportunityEvidence` output — still entirely unbuilt, OD-009). World Model's dedup key (`entity_id`, `signal_type`) was not widened to merge different signal_types onto one event — a separate, larger decision not made this sprint. See ADR-045 |
| `OpportunityLifecycle` / Decay Engine | NOT BUILT IN CODE | `10_OPPORTUNITY_ENGINE.md` relabeled SPECIFIED — NOT IMPLEMENTED (V3.1.4 BATCH-3, OD-009); current `opportunity/engine.py` produces a single-pass `AttentionRecommendation`, no stage machine, no decay accumulation |
| Real (non-demo) API endpoint | BUILT (V3.1.4 BATCH-4); NVDA can be config-gated to a real live opportunity (Sprint 3.6.6C) | `GET /v1/opportunities` (`backend/app/opportunities.py`) — thin adapter over the real `logan_core` pipeline, `schema_version` metadata, `internal_rank_score` never serialized. `/v1/demo/tesla` and `/v1/demo/feed` marked `deprecated=True`, kept functional for the legacy screens. With `STRATUS_LIVE_NVDA_EARNINGS=true` and a valid `FMP_API_KEY`, the NVDA item this endpoint returns can be a genuine FMP-driven opportunity instead of the simulated fixture — see the NVDA live-path config gate row above |
| WebSocket server | NOT BUILT | |

**ADR-034 conflict — resolved in V3.1.4 BATCH-1:** the prior session's known conflict (`community.momentum_score`
influencing `priority_score` both directly and via `global_importance`, and `backend/app/logan_feed.py` exposing
`priority_score` as a public field) is fixed. `internal_rank_score` no longer contains any `community_momentum`
term, `global_importance` derives from `confidence.confidence_score` only, and the public feed response exposes
an ordinal `rank` instead of any score. See `docs/DECISIONS.md` ADR-029/034 and the V3.1.4 BATCH-1 commits
(`049b7cc`, `3c0e27d`, `a922a55`, `879ce0a`, `d47779d`).

### `backend/app/` — historical prototype (ADR-014, untouched)

Still the only thing actually deployed/running: `main.py`, `memory_engine.py`, `memory_models.py`, `models.py`, `data.py`, `entity_registry.py`, `logan_feed.py`, and `logan_demo.py` (the `logan_core` bridge, ADR-022). Not extended with new pipeline logic per CLAUDE.md.

---

## Infrastructure

| Item | State | Notes |
|------|-------|-------|
| Database/hosting | NOT DECIDED (engine); hosting target chosen and LIVE | ADR-006 (database engine) remains open — SQLite stays, on a durable Fly Volume for this beta stage (Sprint 3.6.9 Block 1, ADR-061). Hosting provider Fly.io: **deployed and running** — `stratus-api.fly.dev`, region `iad`, `shared-cpu-1x`/512MB, `stratus_data` 1GB encrypted volume. See the Sprint 3.6.9 Remote STRATUS deployment/mobile-closeout session notes for the full verification record. |
| CI/CD | NOT BUILT | |
| Cloud deployment | **LIVE** — `https://stratus-api.fly.dev` | Deployed (Sprint 3.6.9 Block 1 deployment pass, then the Remote STRATUS mobile closeout). Health check passing; real live NVDA/AAPL opportunities verified over HTTPS; durable notification-token persistence verified across a real machine restart; CORS confirmed non-wildcard. |
| EAS build (mobile) | **EXECUTED** — real preview build produced | `eas.json`'s `preview`/`production` profiles now carry `EXPO_PUBLIC_API_BASE_URL=https://stratus-api.fly.dev` (Sprint 3.6.9 Remote STRATUS mobile closeout); `development` unchanged, still LAN-based. A real `eas build --profile preview --platform ios --non-interactive` succeeded using already-valid, already-provisioned Apple credentials (no interactive Apple step needed) — install link: `https://expo.dev/accounts/garris-engineering-llc/projects/logan-market-mobile/builds/faa2fe54-2889-4be3-ace1-aee44198393d`. Physical-phone acceptance (cellular/off-LAN, dev PC off) remains an owner-performed step. |

---

## What Exists

1. **`logan_core/`** — all 18 pipeline layers implemented with real code and full per-layer unit tests (95 tests), plus a passing Tesla-scenario integration test. Receptors are simulated, not live. Type-checked (mypy), formatted (Black/Ruff).
2. **Mobile app** — Attention Field (depth-of-focus) as the home screen, wired to the real `/v1/opportunities` API with a subordinate Atmosphere background layer, reduced-motion support, and accessibility labels; legacy radial Opportunity Field and other demo screens preserved. ESLint/Prettier/Jest configured (19 tests).
3. **`backend/app/`** — historical FastAPI/SQLite prototype, still running; `GET /v1/opportunities` is now a real thin adapter over `logan_core` (15 tests total).
4. **Architecture documentation** — this v3.1.3 package (28 core files + TriggerEvent framework files + 6 new ML-foundation files), plus `source_material/` (original v1.3 spec). TriggerEvent and OpportunityLifecycle/Decay content relabeled SPECIFIED — NOT IMPLEMENTED in V3.1.4 BATCH-3 (OD-009).
5. **CI** — GitHub Actions workflow running `logan_core`, `backend`, and `mobile` (now including real Jest tests) jobs on push/PR.

---

## What Is Mocked vs. Real

- **Real, tested, code-backed, and live-verified:** `FmpEarningsProvider` (Sprint 3.6.6B) against the real FMP API, and (Sprint 3.6.6C) the full path from that provider through the unmodified `logan_core` pipeline to a real `GET /v1/opportunities` response for NVDA — but only when explicitly enabled via `STRATUS_LIVE_NVDA_EARNINGS=true`; off by default.
- **Real, tested, code-backed, not yet live-verified beyond NVDA:** the remaining 17 `logan_core/` layers, the Tesla pipeline integration test, the Attention Field (wired to real data) and legacy Opportunity Field UI, and the `STOCK_EARNINGS_BEAT` trigger-detection logic itself (deterministic, unit- and integration-tested; proven against both `FixtureEarningsProvider` and, as of 3.6.6C, real FMP data for NVDA specifically — not proven for any other entity or trigger code).
- **Simulated, not live (the default):** every domain receptor (`logan_core/receptors/simulated.py`) — no external market/sports/news/etc. data source is actually polled by default. `/v1/opportunities` is a real pipeline run over simulated input for every entity except NVDA when `STRATUS_LIVE_NVDA_EARNINGS=true` is explicitly set; NVDA itself remains simulated whenever the flag is off, and falls back to simulated even with the flag on if FMP is unreachable, unauthenticated, or the real report doesn't satisfy `STOCK_EARNINGS_BEAT` (Sprint 3.6.6C — see the NVDA live-path row in the table above and ADR-044).
- **Fixture-proven, explicitly not live (Sprint 3.6.6, new category):** `receptors/providers/FixtureEarningsProvider` and the `STOCK_EARNINGS_BEAT` trigger it feeds — deliberately, visibly labeled non-live (`source_id="fixture_earnings_provider"`) so this is never mistaken for real data. Still used by the deterministic unit/contract test suite; unrelated to the live FMP path.
- **Not built at all:** the rest of the TriggerEvent registry (every stocks code besides `STOCK_EARNINGS_BEAT`, and every other domain's registry), TriggerEvent's revision/dedup model, `OpportunityLifecycle`/Decay Engine in code, WebSocket layer, Opportunity Card as fully specified in `22_OPPORTUNITY_CARD_SPEC.md`, auth, all infrastructure/deployment, an Apple-signed device build (configuration present — bundle ID, EAS project ID, dev-client build profile — but not executed in this environment; see `V3.1.4_IMPLEMENTATION_SUMMARY.md`). Live data for any entity/trigger other than NVDA/`STOCK_EARNINGS_BEAT` remains unbuilt — this sprint deliberately did not expand breadth.

---

## Sprint 2A Target State

When Sprint 2A is complete, this document should show:

| Item | Target State |
|------|-------------|
| Stocks receptor (NVIDIA) | REAL — polling live price/news API |
| TriggerEvent: STOCK_EARNINGS_BEAT | REAL — fires on known earnings beat pattern |
| Normalization | REAL |
| World Model (single entity) | REAL — NVDA entity with trigger_events populated |
| Convergence Detector | REAL — fires and emits TriggerEvent |
| Domain Analysis (Stocks) | REAL |
| Reasoning Engine | REAL |
| Opportunity Engine | REAL |
| /v1/opportunities endpoint | REAL |
| Mobile: Opportunity Field (1 node) | REAL — correct brightness, position |
| Mobile: Opportunity Card | REAL — all required fields populated |
| Feedback capture (Dismiss) | REAL — FeedbackSignal logged |
| Learning System (receives signal) | REAL — does not need to write yet |

After Sprint 2A passes:
- Checkpoint 1: All objects match `07_DATA_CONTRACTS.md`
- Checkpoint 2: `hit_quality_score` ≠ `user_value_score` confirmed
- Checkpoint 3: FeedbackSignal → Learning System → MemoryWrite logged

### Sprint 3.6.6 actual progress against this target (2026-08-14)

The table above describes the target state when Sprint 2A is *complete* — left
unchanged. Actual progress this sprint, against each row:

| Item | Sprint 3.6.6 status |
|------|---------------------|
| Stocks receptor (NVIDIA) | PARTIAL — provider abstraction + receptor mapping built and tested; no live provider/credentials yet, so not REAL by this table's own definition |
| TriggerEvent: STOCK_EARNINGS_BEAT | PARTIAL — deterministic, unit- and integration-tested, fires correctly against fixture data; not yet proven against a live report |
| Normalization | REAL (unchanged — reused exactly as-is, not modified this sprint) |
| World Model (single entity) | PARTIAL — `trigger_events` populates and dedups/replaces correctly (tested); only exercised via the fixture path |
| Convergence Detector | NOT BUILT — this named component doesn't exist in code; `StocksTriggerEvaluator` plays an analogous narrow role for this one trigger code only, not a general convergence mechanism |
| Domain Analysis (Stocks) | NOT BUILT — out of scope this sprint |
| Reasoning Engine | REAL (unchanged — reused exactly as-is) |
| Opportunity Engine | REAL (unchanged — reused exactly as-is; correctly receives the trigger-boosted confidence through the existing `Dimensions.confidence` input, no new code in this layer) |
| /v1/opportunities endpoint | NOT WIRED to the new capability — still simulated-only for every entity, NVDA included |
| Mobile: Opportunity Field (1 node) | NOT TOUCHED this sprint |
| Mobile: Opportunity Card | NOT TOUCHED this sprint |
| Feedback capture (Dismiss) | NOT TOUCHED this sprint — out of scope |
| Learning System (receives signal) | NOT TOUCHED this sprint — out of scope |

Checkpoints: Checkpoint 1 partially applies — the *minimal* `TriggerEvent` contract this sprint built
matches `07_DATA_CONTRACTS.md`'s field names where implemented, but deliberately omits most of the full
canonical shape (see docs/DECISIONS.md ADR-042). Checkpoints 2 and 3 were not attempted (out of scope).

See docs/DECISIONS.md ADR-042 for the full decision record.

### Sprint 3.6.6C actual progress (2026-08-15)

Sprint 3.6.6C's scope was narrower than the Sprint 2A target table above: wire the already-proven
(3.6.6B) live NVDA earnings path into the existing `GET /v1/opportunities` endpoint, config-gated and
off by default, with no new triggers and no redesign of the pipeline. Progress against that specific
scope:

| Item | Sprint 3.6.6C status |
|------|----------------------|
| `/v1/opportunities` NVDA live path | REAL, config-gated (`STRATUS_LIVE_NVDA_EARNINGS`, default off) — live-verified against the real FMP API; returns exactly one NVDA item either way (simulated or live), never both |
| Trigger-gated substitution | REAL — the live NVDA fixture is substituted only when `STOCK_EARNINGS_BEAT` actually fires (verified via `evaluate_earnings_beat_condition` pre-check, shared with `StocksTriggerEvaluator`); a valid-but-non-beating report falls back to the simulated fixture, not a fabricated live opportunity |
| Disabled-mode trace parity | REAL — `PipelineDependencies.trigger_detector` is only constructed when the flag is on, so the disabled-mode `ExecutionTrace` is byte-for-byte identical to every pre-3.6.6C caller for every entity |
| All other entities/domains | NOT TOUCHED — still simulated-only, unaffected by this sprint regardless of the flag |
| Other stocks trigger codes (`STOCK_EARNINGS_MISS`, etc.) | NOT BUILT — explicitly out of scope; stopped here per the sprint's boundary instruction to report before adding more triggers |

See docs/DECISIONS.md ADR-044 for the full decision record, including the disabled-mode-trace and
non-fire-substitution fixes made during implementation review, and a cross-suite test-isolation issue
(`backend/app/config.py`'s import-time `load_dotenv()` leaking a real local `FMP_API_KEY` into a
`logan_core` test that assumed a clean environment) found and fixed along the way.

### Sprint 3.6.6D actual progress (2026-08-18)

Owner-directed scope: two additional stocks trigger codes plus a foundation for resolving multiple
TriggerEvents on one event, per an explicit confidence-model decision (fixed registry constants only,
strongest-trigger-wins on convergence, no summing, no invented weights, no ML/calibration redesign yet).

| Item | Sprint 3.6.6D status |
|------|----------------------|
| `STOCK_EARNINGS_MISS` | REAL — same architecture as `STOCK_EARNINGS_BEAT` (same provider fields, same evaluator, same downstream pipeline), fixture/unit/integration-tested; not yet live-proven (no real NVDA report has missed since this shipped) |
| `STOCK_EARNINGS_IN_LINE` | REAL — fires with `0.0` confidence contribution per the registry ("used to close hypotheses"); evidence preserved on the event even though it doesn't move confidence |
| ConvergenceDetector foundation | REAL, narrow — resolves multiple `TriggerEvent`s on one event to the single strongest contribution; not the canonical Hit-Detection-layer Convergence Detector (unbuilt); World Model's merge key unchanged |
| Confidence-model decision | APPLIED — no new weights invented; `STOCK_EARNINGS_BEAT`'s `+0.22` unchanged; MISS/IN_LINE's `+0.20`/`0.0` taken verbatim from the registry; convergence resolves to strongest, never summed |
| Other stocks trigger codes (`STOCK_GUIDANCE_RAISED`, `STOCK_OPTIONS_FLOW_SURGE`, etc.) | NOT BUILT — each needs data not currently fetched; explicitly out of scope, per "prefer reuse over adding provider complexity" |
| Other domains | NOT TOUCHED — explicitly out of scope this sprint |

See docs/DECISIONS.md ADR-045 for the full decision record, including which trigger codes were considered
and rejected for lack of provider data, and the known limitation around companion/negative triggers
(e.g. the unimplemented `STOCK_EARNINGS_QUALITY_WARNING`) that the strongest-not-summed rule doesn't yet
model correctly.

### Sprint 3.6.6 close-out (2026-08-19 through 2026-08-21) — Sprint 3.6.6 now functionally complete

Everything from Sprint 3.6.6E onward, closing out the sprint. Owner-directed throughout: STRATUS Watch real
push notifications, then behavioral personalization feeding Watch eligibility, without touching Watch
thresholds elsewhere or trigger/domain breadth.

| Item | Status |
|------|--------|
| Confidence-inflation / overclaimed-personalization wording fix (Sprint 3.6.6E) | REAL — stopped repeated-polling confidence inflation; corrected narrative wording that overclaimed personalization the system couldn't actually verify |
| STRATUS Watch push notifications, mobile registration (Sprint 3.6.6F + mobile) | REAL — Expo push token registration/tap-to-open (mobile), backend push-notification support reusing the existing `interruption == "alert"` gate as eligibility (no new threshold invented at the time) |
| Notification badge/push state coherence (Sprint 3.6.6G) | REAL — fixed a foreground-refresh root cause; badge and push state now coherent |
| Push notification wording (Sprint 3.6.6H) | REAL — concise, non-redundant wording |
| Engagement fixture timing correction (Sprint 3.6.6I, ADR-046) | REAL — fixed a same-timestamp fixture artifact that was silently inflating `lifecycle_state`/`engagement_velocity` for 10 of 11 simulated entities; also the trace that first surfaced FED alerting with a fully generic `personal_relevance=0.50` (the open question ADR-049 later resolved) |
| Behavioral interaction capture: card opens, dwell time, notification taps (ADR-047) | REAL — new `POST /v1/interactions` route, `logan_feed.record_interaction()`, mobile dwell-tracking/tap-capture, routed through the existing (previously call-less) `Orchestrator.run_feedback_loop()` end-to-end into `MemoryStore` |
| Process-lifetime persistent `UserModel`; repeated behavioral evidence → inferred interests (ADR-048) | REAL — `_get_user_model()` (mirrors `_get_orchestrator()`'s singleton pattern) seeds once and rebuilds via `UserModelBuilder.build()` on every later request; `_fold_behavioral_evidence()` folds ≥2 repeated `interested`-intent `feedback_record`s per `(domain, entity_id)` into `established_behaviors`/`domain_preferences`/`Interest(source="inferred")`; explicit holdings/interests and `inferred_expertise` always preserved unchanged. Orchestrator ownership restored (`run_feedback_loop()`'s `content` now also accepts a callable, so structured content can be built from the interpreted `FeedbackSignal` without a direct `feedback_engine`/`learning_engine` call or a second interpretation) |
| Explicit vs. inferred relevance separation, auditable (ADR-048) | REAL — `ReasoningResult.connected_entities_explicit`/`connected_entities_inferred` (additive fields); `OpportunityEngine` keeps the original 0.6 relevance bump for explicit connections, bounds inferred-only connections to 0.5; both logged into `DecisionTraceEntry.evidence` |
| STRATUS Watch Personal and Exceptional eligibility routes (ADR-049) | REAL — `PolicyEngine`'s `communication_mode="alert"` decision replaced the old single `urgency >= 0.7` gate with two explicit routes (Personal: explicit or mature-inferred relevance + sufficient event quality; Exceptional: very-high urgency/confidence/novelty regardless of personalization), both reusing existing anchors, no new numeric policy invented. `PolicyEngine` remains the sole communication gate; `PrioritizationEngine`'s fatigue/cooldown/rank-score logic is unchanged and retains final authority over the actual `interruption` value (see ADR-049's own consequences for the live trace proving this, including a case where a qualifying Personal-route item still lands as `digest`, not `alert`, because of Prioritization's separate rank-score gate) |
| Test growth this close-out | `logan_core` 162 → 197 (+35), `backend` 31 → 82 (+51, spanning notifications/badge/engagement-timing/interactions/persistence/routes) |

**Known limitations carried into Sprint 3.6.7, not resolved this sprint** (see `SESSION_NOTES.md`'s Sprint
3.6.7 starting-point section for the full list): a qualifying Personal-route item can still land as `digest`
instead of `alert` because Prioritization's `internal_rank_score >= 0.6` gate has final authority, and whether
that's the right end-state for mature inferred relevance is undecided; impression/exposure semantics
(server-surfaced vs. client-rendered) remain unresolved and behavioral relevance is not yet normalized against
exposure; Personal and Exceptional routes currently share identical fatigue/cooldown handling; Exceptional's
thresholds are validated only against the current simulated fixture distribution, not broad real-world
signals; `UserModel` persistence remains process-lifetime only (no durable store); `MemoryStore.query()`
remains single-user-only, not multi-user-safe; `internal_rank_score >= 0.6` is a soft, uncontracted coupling
between `PolicyEngine` and `PrioritizationEngine`; FIELD BIAS learning, Ask STRATUS linkage, broader trigger
expansion, and broader ML calibration remain deferred, unchanged from prior sprints.

See docs/DECISIONS.md ADR-046 (engagement fixture timing), ADR-047 (behavioral-personalization foundation),
ADR-048 (UserModel persistence + source-aware learning), and ADR-049 (Watch Personal/Exceptional routes — the
final Watch-policy decision record for Sprint 3.6.6) for the full decision records.

### Sprint 3.6.7 Block 1 (2026-08-21) — Personal-route authority rule + generalized stock-signal architecture

Branch `feat/sprint-3.6.7-stock-signal-expansion`, cut from the clean Sprint 3.6.6 integration HEAD.

| Item | Status |
|------|--------|
| Personal-route rank-score authority rule (ADR-050) | REAL — `PrioritizationEngine.prioritize()`'s `visibility`/`interruption` are now computed independently past the fatigue/cooldown/permitted vetoes (all three unchanged); `interruption="alert"` whenever `PolicyEngine`'s `communication_mode=="alert"`, no longer gated a second time by the `internal_rank_score>=0.6` visibility tier. Resolves the exact gap ADR-049 closed out with |
| Generalized stock-signal architecture (ADR-051) | REAL — `StocksTriggerEvaluator.evaluate()` dispatches by `signal_type`; Provider/Receptor/Evaluator pattern proven for earnings now has two more implementations sharing the same shape |
| `STOCK_PRICE_MOVE_SIGNIFICANT` (ADR-051) | REAL, live-verified — FMP `/quote` endpoint; fixture-proven full-pipeline real alert (NVDA holding + qualifying move → `interruption="alert"`) |
| `STOCK_ANALYST_UPGRADE` / `STOCK_ANALYST_DOWNGRADE` (ADR-051) | REAL, live-verified — FMP `/grades` endpoint, trusts the provider's own action classification |
| `STOCK_GUIDANCE_RAISED`/`LOWERED`, `STOCK_OPTIONS_FLOW_SURGE` | Still NOT BUILT — no reliable provider data (unchanged finding from ADR-045) |
| "Unusual volume" / "volatility spike" | NOT BUILT, no registry code exists for either — would require inventing an unbacked `confidence_contribution`, forbidden by the standing Sprint 3.6.6D rule |
| Wiring new signals into `/v1/opportunities` live path | Deliberately deferred to Block 2 — `WorldModel.process()`'s `(entity_id, signal_type)` dedup key would silently drop one of two simultaneous live signals for one entity today |
| Test growth | `logan_core` 197 → 248 (+51: 5 for ADR-050, 46 for ADR-051) |

### Sprint 3.6.7 Block 2 (2026-08-22) — signal convergence (STOCK_CONVERGENCE_MULTI_SOURCE)

Same branch, resuming after a PC crash interrupted the first Block 2 attempt at the reconnaissance stage
(verified clean restart from `6fe4fdd`, no partial work existed). Owner-selected approach: **Option 1 — a
parallel convergence tracker**, not a widened `WorldModel` dedup key (rejected — would erase the existing
per-signal-type dedup/corroboration semantics `world_model/model.py` depends on).

| Item | Status |
|------|--------|
| `STOCK_CONVERGENCE_MULTI_SOURCE` (ADR-052) | REAL — moved from SPECIFIED — NOT IMPLEMENTED to BUILT. New `logan_core/convergence/tracker.py`'s `StockConvergenceTracker`: persistent, process-lifetime, watches `TriggerEvent`s trigger detection already produces, fires when ≥3 distinct `signal_type`s converge on one entity within a 30-minute window (windowed on `detected_timestamp`, not `event_timestamp`) |
| `WorldModel`'s `(entity_id, signal_type)` dedup/corroboration semantics | UNCHANGED — same file, same behavior, verified by the full pre-existing `test_world_model.py` suite passing unmodified |
| Coherent multi-signal opportunities (ADR-052) | REAL — `Orchestrator.run()` now merges multiple distinct-signal_type `EnrichedEvent`s for one entity into one coherent opportunity (new `_collapse_duplicate_event_ids`/`_merge_entity_events`/`_attach_trigger` helpers in `orchestrator/pipeline.py`) instead of the last-processed `raw_signals` entry silently dropping the others (ADR-051 finding 5) |
| Live wiring of Block 1 signals into `/v1/opportunities` | REAL — `backend/app/logan_feed.py`'s `_run_feed_pipeline()` now fetches and feeds all qualifying live NVDA signals (earnings, price move, analyst grade — 1 to 3, whichever genuinely fire this poll) into one `orchestrator.run()` call, under the same `STRATUS_LIVE_NVDA_EARNINGS` flag |
| Test growth | `logan_core` 248 → 265 (+17: 11 tracker unit tests, 6 pipeline integration tests; plus a pre-existing unrelated date-dependent flake fixed while running the full suite). `backend` 82 → 89 (+7, live wiring/convergence gating) |

See docs/DECISIONS.md ADR-052 for the full decision record, including why `event_timestamp` was rejected as
the convergence window basis in favor of `detected_timestamp`, and the duplicate/repeated-polling and
repeated-active-episode suppression mechanisms.

See docs/DECISIONS.md ADR-050 (Personal-route authority rule), ADR-051 (generalized stock-signal
architecture), and ADR-052 (signal convergence) for the full decision records.

### Sprint 3.6.7 Block 3 (2026-08-22) — persistent behavioral personalization, exposure semantics, live convergence verification

Same branch. Closes the loop ADR-047/048 explicitly left open: real exposure/impression semantics, durable
persistence surviving a backend restart, a deterministic behavioral relevance model (maturity/decay/
exposure-fatigue), matured-relevance Watch integration, and (as one acceptance item) the Block 2 live
convergence verification carryover.

| Item | Status |
|------|--------|
| Exposure/impression semantics (ADR-053) | REAL — `InteractionType` gains `impression`/`ask_followup`; `RecordType` gains `exposure_record`, structurally excluded from behavioral-evidence folding (impressions alone cannot create relevance). New `LearningEngine.process_exposure()`/`Orchestrator.run_exposure_loop()`. Mobile: `useImpressionTracking.ts` fires on `AttentionField`'s existing `focusedId` change |
| Durable persistence (ADR-053) | REAL, config-gated (`STRATUS_PERSIST_MEMORY`, default off) — `MemoryStore` optionally backed by a new local SQLite file (`backend/data/stratus_state.db`, schema-versioned, bounded-history compaction). `UserModel` remains derived state, rebuilt from persisted records — never stored directly. Disabled mode (every existing test) is byte-for-byte the old in-memory-only store |
| Deterministic behavioral relevance model (ADR-053) | REAL — maturity scaling (`+0.02`/qualifying event beyond `MIN_REPEAT_EVIDENCE`, capped `+0.10`, weight capped `0.90`), recency-based decay (14-day half-life, pruned below `0.10`), exposure-fatigue dampening (weak, bounded, only on an existing inferred interest, only after a real recency gap since last engagement) — all in `user_model/model.py`, none of it LLM-driven |
| Matured relevance -> Personal route (ADR-053) | REAL — `ReasoningResult.inferred_relevance_strength` (new) + `OpportunityEngine._scale_inferred_relevance()` replace the old flat `0.5` inferred-connection floor with a `[0.5, 0.59]` range that scales with evidence maturity, strictly below the explicit `0.6` tier (ADR-048 invariant unchanged). `PolicyEngine`/`PrioritizationEngine` (ADR-049/050) untouched -- mature evidence can now measurably move an event within existing, unmodified Watch thresholds |
| Live convergence verification (ADR-054) | REAL — `logan_core/live_verification/nvda_convergence.py`, run live 2026-08-22: only 1 of 3 NVDA signal types fired (earnings beat), so `STOCK_CONVERGENCE_MULTI_SOURCE` honestly did not qualify; full pipeline still produced a real alert from the earnings signal alone |
| Test growth | `logan_core`/`backend` 354 → 405 (+51). `mobile` 88 → 94 (+6) |

See docs/DECISIONS.md ADR-053 (persistent behavioral personalization) and ADR-054 (live convergence
verification) for the full decision records.

### Sprint 3.6.7 Block 4 (2026-08-22) — contextual Ask STRATUS, ASK_FOLLOWUP high-intent evidence

Same branch. Wires Block 3's `ask_followup` concept into a real, grounded, per-opportunity Ask STRATUS flow.

| Item | Status |
|------|--------|
| Contextual Ask contract (ADR-055) | REAL — `AskRequest`/`AskResponse` gain additive optional `event_id`/`session_id`/`grounded` fields on the existing `/v1/ask` route (no second endpoint). Generic (no-context) path unchanged and, for the first time, test-covered |
| Authoritative opportunity rehydration (ADR-055) | REAL — new `OpportunityContext`/`OpportunityContextCache` (`backend/app/ask_context.py`), built from the same `PipelineResult` that already produces each `FeedItem`, refreshed every `_run_feed_pipeline()` call. Client sends only `event_id`; an unresolvable one returns an honest "no current context" answer, never fabricated |
| Deterministic grounded answers (ADR-055) | REAL — `backend/app/ask_engine.py`, keyword-classified against real `DeliveredItem`/`ConclusionConfidence`/`TriggerEvent`/`Dimensions` fields. No LLM anywhere in this codebase (confirmed by inspection); adding one would be a new dependency requiring separate confirmation, not made here |
| `ASK_FOLLOWUP` wiring (ADR-055) | REAL — a successful contextual question records real behavioral evidence via the existing Block 3 `record_interaction()`/`FeedbackEngine` path (0.80 confidence tier, unchanged). Invalid opportunity / empty message / generic questions never record engagement |
| Session continuity + feedback-loop cap (ADR-055) | REAL — bounded, process-lifetime session store (structural anchor only, no raw transcript persisted); at most one `ASK_FOLLOWUP` contribution per (session, opportunity), verified not to weaken the pre-existing Block 3 short-window dedup |
| Mobile wiring (ADR-055) | REAL — one "Ask STRATUS about this" button per opportunity card (`Vessel.tsx`), `ask.tsx` reads `eventId`/`entityId`/`displayName`/`domain` via Expo Router params (new pattern for this app), contextual first-state copy/starters, honest "Discussing {entity}" chip only when real context exists |
| Test growth | `logan_core`/`backend` 405 → 446 (+41). `mobile` 94 → 100 (+6) |

See docs/DECISIONS.md ADR-055 for the full decision record, including the acceptance-scenario proof and why
AAPL stands in for the task description's "AMD" (not a real fixture entity in this codebase).

### Sprint 3.6.8 Block 1 (2026-08-22) — grounded LLM Ask STRATUS

Same branch, continuing directly from the Block 4 closeout commit `1e67120`. First real LLM integration in
this codebase — confirmed by inspection (no LLM call anywhere in `backend/`, `logan_core/`, or `mobile/`
prior to this block), and added only after explicit owner confirmation of both the new `anthropic` SDK
dependency and the new `ANTHROPIC_API_KEY` secret per CLAUDE.md's collaboration model.

| Item | Status |
|------|--------|
| Vendor-agnostic provider abstraction (ADR-056) | REAL — `AskLlmProvider` Protocol (`ask_llm_provider.py`), `FixtureAskLlmProvider` test double (`ask_llm_fixture.py`), `AnthropicAskLlmProvider` (`ask_llm_anthropic.py`, model `claude-sonnet-5`, owner-approved). Only `ask_llm_anthropic.py` imports the `anthropic` package anywhere in this codebase |
| Structured grounding (ADR-056) | REAL — `build_system_prompt()` renders only real `OpportunityContext` fields (the same ones Block 4's deterministic engine uses); explicitly instructs the model not to invent market facts/prices/confidence values and not to contradict the given classification; restates the ADR-002/010 advice boundary independently |
| Deterministic fallback (ADR-056) | REAL — `generate_grounded_answer()` (`ask_engine.py`) falls back to the unchanged Block 4 `answer_question()` on disabled/unavailable/any `AskLlmProviderError` (network, timeout, rate limit, refusal, empty/malformed response). No LLM failure mode can break Ask STRATUS |
| Config gating (ADR-056) | REAL, default off — `STRATUS_LLM_ASK` (`config.llm_ask_enabled()`), same rollout pattern as `STRATUS_LIVE_NVDA_EARNINGS`/`STRATUS_PERSIST_MEMORY`. `get_ask_llm_provider()` is lazy, thread-safe, memoized; degrades to `None` (never crashes) on a missing key or construction failure |
| Prompt-injection hygiene (ADR-056) | REAL — the user's question is sent as a structurally separate `user` message, never concatenated into the system prompt; verified directly against a captured provider call |
| `ASK_FOLLOWUP` semantics (ADR-056) | UNCHANGED, verified — recording depends only on "did a real answer get produced," decoupled from LLM-vs-deterministic; an LLM-failure-then-fallback question records exactly one `ask_followup` event |
| Public contract (`AskRequest`/`AskResponse`) | UNCHANGED — `used_llm`/`llm_model` stay internal to `GroundedAnswer`; no mobile contract impact, no mobile UI change this block |
| Real `ANTHROPIC_API_KEY` value | NOT SET — `backend/.env` has no real key; `STRATUS_LLM_ASK` defaults off regardless, so behavior is unchanged from Block 4 until the owner opts in locally |
| Test growth | `backend` 141 → 179 (+38). `logan_core` unaffected (306, untouched by this block) |

See docs/DECISIONS.md ADR-056 for the full decision record.

### Sprint 3.6.8 Block 2 (2026-08-22) — production user boundaries

Same branch, continuing directly from the Block 1 closeout commit `eb7ad40`. Recon before writing any code
found most of `logan_core` was already multi-user-safe by construction (`Orchestrator.run()`/
`run_feedback_loop()`/`run_exposure_loop()` already take an explicit `user_id`; `MemoryRecord.user_id` is
required per ADR-033; `PrioritizationEngine`'s `AttentionState` was already a per-user dict internally) — the
real gap was `backend/app/` never threading any real per-request identity through, plus one genuine
pre-existing bug: `orchestrator/pipeline.py`'s `run()` called `memory_store.query(entities=...)` with no
`user_id` filter at all, leaking every user's `feedback_record`s for a shared entity into every other user's
`UserModel` rebuild.

| Item | Status |
|------|--------|
| Identity transport (ADR-057, mode-awareness added ADR-064) | REAL — `X-Stratus-User-Id` header, resolved via `backend/app/user_context.py`'s `resolve_user_id()` FastAPI dependency; not authentication. Demo/development mode unchanged (absent → `LOCAL_FOUNDER_USER_ID`). Beta/production mode (Sprint 3.6.9, ADR-064): the founder constant is **never** reachable via a client-supplied header — a real, found-in-review exposure (`LOCAL_FOUNDER_USER_ID` is the public string `"demo_user"`, so any hosted caller could obtain the founder's real data with no guessing) — both an absent header and an explicit `"demo_user"` header now resolve to a distinct, blank-seeded `BETA_ANONYMOUS_USER_ID`. Also caps accepted header length at 128 chars. Wired into every user-facing route |
| `MemoryStore.query()`/`.all()` user-scoping (ADR-057) | REAL — `user_id` is now a required parameter, no default, no "all users" mode. Closes the real cross-user leak in `orchestrator/pipeline.py`'s `run()` at its root. No SQLite schema change — the `user_id` column/index already existed (Sprint 3.6.7 Block 3) |
| Per-user process-lifetime state (ADR-057) | REAL — `backend/app/logan_feed.py`'s `_user_model`/`OpportunityContextCache`/`_ask_sessions` singletons converted to per-`user_id` dicts (the OpportunityContext cache closes the sharpest read-side leak: it carries personalized `personal_relevance`/`connection_basis`/`is_new_for_user`). The shared World Model/Orchestrator instance is deliberately **not** duplicated per user — two users see the identical `event_id` for the identical real-world fact; only personalization is user-scoped |
| New-user seeding (ADR-057) | REAL — only `LOCAL_FOUNDER_USER_ID` gets the seeded NVDA holding/AI_SECTOR interest; every other `user_id` starts genuinely blank (`UserModelBuilder.seed()`'s own defaults), never a copy of the founder's portfolio |
| Push notification per-user isolation (ADR-057) | REAL — `backend/app/notifications.py`'s registered-token/dispatched/reviewed-event-id state converted to per-`user_id` dicts; the background poller now dispatches once per registered user against that user's own alert-eligible items |
| Review point: `record_interaction()` ownership | REVIEWED, NO CHANGE NEEDED — confirmed by inspection (direct read + repo-wide grep) that it has gone through `Orchestrator.run_feedback_loop()`/`run_exposure_loop()` since ADR-047; no bypass to `feedback_engine.interpret()`/`learning_engine.process_feedback()` exists |
| Review point: `DomainPref(weight=0.5)` | REVIEWED, DOCUMENTED NOT REMOVED — confirmed inert (required contract field, no consumer anywhere in the pipeline, never updated after creation); left unchanged with explicit comments at both construction sites rather than inventing new meaning or silently removing a required field |
| Mobile per-device identity | **BUILT (Sprint 3.6.9, ADR-064)** — `mobile/lib/identity.ts` generates a real per-install UUID (`expo-crypto`), persisted via `expo-secure-store`; `mobile/lib/apiClient.ts`'s centralized `fetchJson()` attaches it to every request as `X-Stratus-User-Id`. Real identity, still not authentication. Requires a new EAS build to reach the already-installed phone |
| API request rate limiting | **BUILT (Sprint 3.6.9, ADR-064)** — `backend/app/rate_limit.py`, in-memory fixed-window per `(route, user_id)`, no new infrastructure. Applied to `/v1/opportunities` (30/60s) and `/v1/ask` (20/5min — the sharper risk, since a grounded question can trigger a real metered Anthropic call) |
| Test growth | `backend` 179 → 193 (+14, `test_multi_user_isolation.py`); Sprint 3.6.9 adds `test_user_context.py` (14)/`test_rate_limit.py` (8) on top of later growth. `logan_core` unchanged (306, migrated call sites covered by the existing suite) |

See docs/DECISIONS.md ADR-057 for the full decision record, including both review-point resolutions in full.

### Sprint 3.6.8 Block 3 (2026-08-23) — bounded conversational Ask STRATUS

Same branch, continuing directly from the Block 2 closeout commit `9f8807e`. Recon found the mobile client
already renders and resends a real multi-turn conversation; the gap was entirely server-side.

| Item | Status |
|------|--------|
| Bounded conversation history (ADR-058) | REAL — `_AskSession.history`, capped at 6 (question, answer) pairs / 4000 chars, full-pair eviction preserving the strictly-alternating role invariant. Process-lifetime only, same as the rest of `_ask_sessions` — no durable raw-chat store introduced |
| Opportunity-anchor-change reset (ADR-058) | REAL — `set_ask_session_event()` clears history on a genuine `event_id` change within a session; a no-op when the same anchor is resent (mobile's own behavior) |
| Authoritative-context-wins invariant (ADR-058) | REAL, structurally enforced — `build_system_prompt(context)` takes only `context`; history can never be concatenated into it. New prompt text states current context always wins over any earlier turn |
| No invented signal ranking (ADR-058) | REAL — system prompt now explicitly instructs the model to say a ranking isn't supported absent real convergence, matching the deterministic path's own pre-existing `_dominant_signal_answer` discipline |
| Provider abstraction (ADR-058) | REAL, additive — new vendor-neutral `ConversationTurn`; `AskLlmProvider.generate()` gained a defaulted `history` param; only `AnthropicAskLlmProvider` translates it into Anthropic's own message shape |
| Deterministic fallback mid-conversation (ADR-058) | REAL — the deterministic path answers standalone (a reasoned scope boundary, not an oversight); whichever path produced the real answer is what enters history |
| ASK_FOLLOWUP bound under conversational depth (ADR-058) | VERIFIED UNCHANGED — the existing per-(session, opportunity) cap already covered this; new tests prove 12 real turns and a mid-session opportunity switch both stay correctly bounded |
| Mobile | ZERO CODE CHANGES — `ask.tsx` already supports everything Block 3 needed; full mobile suite (100 tests), `tsc --noEmit`, `eslint` re-run and confirmed clean |
| Test growth | `backend` 193 → 223 (+30, `test_ask_conversation.py`). `logan_core` unaffected (306) |

See docs/DECISIONS.md ADR-058 for the full decision record.

### Sprint 3.6.8 Block 4 (2026-08-23) — beta-readiness hardening

Same branch, continuing directly from the Block 3 closeout commit `0d0b771`. A recon-driven hardening pass
across the fully integrated provider→pipeline→mobile→conversational-Ask→behavioral-learning loop, governed
mid-session by an explicit owner rule: production opportunity generation must be live-data-first, fixtures
belong only in tests/acceptance scenarios, and no threshold may be lowered or signal fabricated to force a
live check to pass.

| Item | Status |
|------|--------|
| Notification dispatch per-user isolation bug (ADR-059) | FIXED — `dispatch_eligible_notifications()` claimed one user's failure couldn't stop another's dispatch, but only the push send itself was guarded; the per-user pipeline call was not. Widened the guard to the whole per-user body; new test proves the fix |
| Notification poller event-loop-blocking bug (ADR-059) | FIXED — `_notification_poll_loop()` called the fully synchronous, per-user-pipeline-running `dispatch_eligible_notifications()` directly from an `async def` coroutine, unlike every HTTP route (already threadpooled by FastAPI). Wrapped in `asyncio.to_thread()`. Currently inert (live flag off by default) but a real, latent hazard once enabled |
| FMP API-key redaction (ADR-059) | ADDED, defensive — error messages built from raw FMP response text now strip the real API key before ever being raised/logged, satisfying "never log API keys" by construction |
| Minimum viable Ask STRATUS observability (ADR-059) | ADDED — one log line per successfully-grounded turn (user_id, entity_id, which path answered) — routing metadata only, never question/answer text |
| Restart-safety matrix (ADR-059) | DOCUMENTED, executable — MemoryStore records survive a persisted restart; AttentionState/Ask history/notification-token state do not, regardless of the persistence flag |
| Multi-user regression through the full conversational stack (ADR-059) | RE-PROVEN — Block 2's guarantees re-tested against Block 3's real multi-turn path, including a shared session_id string between two users |
| Full integrated acceptance path (ADR-059) | PROVEN, fixtures throughout — no explicit holding → real opportunity → limited relevance → conversational Ask → bounded ASK_FOLLOWUP → real persisted restart → correctly-scoped survival → matured-but-bounded relevance → second user unaffected |
| Live-data-first inventory (ADR-059) | REPORTED, NOT FIXED — exactly one production fixture call site (`_run_feed_pipeline`'s `simulated_fixtures()`), 10 of 11 entities always simulated, only NVDA has any live path (off by default). TSLA/AAPL could extend the existing FMP integration without a new vendor; MARKETS/OIL need a product decision; BTC/FED/NFL/MUSIC/POLY each need a genuine new vendor per domain — none added, none guessed at |
| EAS/TestFlight readiness recon (ADR-059) | REPORTED, NOT FIXED — `eas.json` has no `EXPO_PUBLIC_API_BASE_URL` for any build profile; a real `preview`/`production` build today would ship with a stale, unreachable local dev IP baked in. Blocked on ADR-006's own open hosting decision, not fixed with a guessed value |
| `27_SECURITY_PRIVACY_COMPLIANCE.md` correction (ADR-059) | REAL — the stale "no way for a second distinct user to exist" claim replaced with a precise description of real backend state isolation (Block 2) vs. the continued total absence of authentication — the `X-Stratus-User-Id` header is never verified |
| Test growth | `backend` 223 → 227 (+4: 1 dispatch-isolation test, 3 in `test_beta_hardening.py`). `logan_core` unaffected (306) |

See docs/DECISIONS.md ADR-059 for the full decision record, including the complete live-data and
EAS-readiness inventories.

### Sprint 3.6.8 Block 5 (2026-08-23) — live-data transition foundation

Same branch, continuing directly from the Block 4 closeout commit `e1c24b5`. Removes the NVDA-only
limitation from the already-approved FMP provider architecture, establishing one reusable live equities path
for any configured ticker, plus an explicit production-vs-demo runtime boundary. Recon found the NVDA
coupling was entirely a `backend/app/` wiring artifact — `logan_core` was already fully entity-generic.

| Item | Status |
|------|--------|
| `config.live_stock_tickers()` (ADR-060) | REAL — new `STRATUS_LIVE_STOCK_TICKERS` (comma-separated), deterministic parsing/validation, no network call. Falls back to the original `STRATUS_LIVE_NVDA_EARNINGS` flag exactly when unset — every pre-Block-5 deployment/test unaffected |
| Generalized live equities functions (ADR-060) | REAL — `_live_earnings_raw_signal`/`_live_price_move_raw_signal`/`_live_analyst_grade_raw_signal` accept any ticker; `logan_core`'s receptor/provider/trigger/convergence layers required zero changes, already entity-generic |
| TSLA-corroboration-into-live-signal bug (ADR-060) | FIXED — a real live TSLA earnings signal could previously have been silently joined by a fabricated simulated corroborating signal; guarded on `live_substituted` |
| Simulated-primary + live-secondary blending bug (ADR-060) | FIXED — a live price-move/grade signal could previously have been spliced onto a simulated primary earnings signal; an opportunity is now always fully live or fully simulated, never blended |
| `config.live_data_only_mode()` (ADR-060) | REAL — new `STRATUS_RUNTIME_MODE=live/beta/production`; in that mode `fixtures` starts empty, so unsupported domains and non-qualifying live tickers are both honestly absent, never simulated-substituted |
| Data provenance (ADR-060) | CONFIRMED SUFFICIENT, NOT NEW — the existing `source_id` field (`"fmp"` vs. simulated source IDs) and the existing print-based log convention already serve as the live/fixture audit trail |
| Live verification (ADR-060) | REAL, run 2026-08-23 against the real FMP API — NVDA earnings beat fired; TSLA earnings *miss* fired (correctly did not go live through the real pipeline wiring, confirming the deliberate BEAT-only gap with real data); AAPL earnings beat + real analyst downgrade fired and combined into one live opportunity. No convergence fired for any ticker (honest, not forced) |
| Multi-stock convergence entity isolation (ADR-060) | VERIFIED, NO CODE CHANGE — `StockConvergenceTracker` was already keyed per-`entity_id` internally; new tests prove no cross-symbol contamination under the generalized path |
| Personalization separation (ADR-060) | VERIFIED — a new user does not inherit interest in a live-configured ticker merely because the backend fetches it; founder seed behavior remains exactly as Block 2 established |
| Test growth | `backend` 227 → 267 (+40: `test_config_live_stocks.py` 17, `test_live_equities.py` 23). `logan_core` unaffected (306 — the generalization lived entirely in `backend/app/`) |

**Fixture-backed runtime inventory (complete, as of this block):**

| Entity | Domain | Current runtime source | Live provider available? | Simulated fixture used? | Live-ready after Block 5? | Remaining decision |
|--------|--------|------------------------|---------------------------|--------------------------|----------------------------|---------------------|
| NVDA | stocks | Live (earnings; price/grade when earnings also live) when configured, else simulated | Yes — FMP (existing) | Yes, as fallback | Yes | None — fully live-capable |
| TSLA | stocks | Live (earnings BEAT only) when configured, else simulated | Yes — FMP (existing) | Yes, as fallback | Yes for BEAT; MISS/IN_LINE not yet wired to substitute | Extend substitution to MISS/IN_LINE (parameterization, not a new vendor) |
| AAPL | stocks | Live (earnings; price/grade when earnings also live) when configured, else simulated | Yes — FMP (existing) | Yes, as fallback | Yes | None — fully live-capable |
| MARKETS | stocks | Simulated only | No — represents aggregate market breadth, not a single ticker | Yes | No | Product decision: what real instrument/index does "MARKETS" track? |
| OIL | stocks | Simulated only | No — represents a commodity, not a single ticker | Yes | No | Product decision: what real instrument does "OIL" track (e.g. a futures symbol)? |
| BTC | crypto | Simulated only | No — no crypto provider integrated | Yes | No | Owner vendor decision — new external provider required |
| FED | news | Simulated only | No — no news/macro provider integrated | Yes | No | Owner vendor decision — new external provider required |
| NFL | sports | Simulated only | No — no sports data/odds provider integrated | Yes | No | Owner vendor decision — new external provider required |
| MUSIC | social | Simulated only | No — no social/culture trend provider integrated | Yes | No | Owner vendor decision — new external provider required |
| POLY | poly | Simulated only | No — no prediction-market provider integrated | Yes | No | Owner vendor decision — new external provider required |
| AI_SECTOR | social | Simulated only | No — no social/trend provider integrated | Yes | No | Owner vendor decision — new external provider required |

See docs/DECISIONS.md ADR-060 for the full decision record, including the exact live-verification results
and both bug fixes.

### Sprint 3.6.9 Block 1 (2026-08-23) — Remote STRATUS: Fly.io hosting readiness, durable notification state, mobile release-URL invariant

First block of the post-3.6.8 gap analysis's Sprint 3.6.9. Makes the repository deployment-ready for
Fly.io, closes the two CRITICAL beta blockers the gap analysis identified that were purely a matter of
implementation (no owner decision needed beyond the hosting/persistence choices already made): registered
push tokens/dedup state surviving a redeploy, and a mobile release build never silently pointing at a
private LAN address. Does not deploy anything — no Fly account, app, volume, or secret was created.

| Item | Status |
|------|--------|
| `Dockerfile`/`.dockerignore`/`fly.toml` (ADR-061) | NEW — repo root, build context includes `logan_core` as a sibling of `backend/` (preserves the existing `sys.path` bridge, ADR-022, rather than refactoring imports). `fly.toml` pins `min_machines_running = 1` / `auto_stop_machines = false` so the in-process notification poller keeps running |
| `config.legacy_memory_db_path()`/`notification_store_db_path()` (ADR-061) | NEW — both SQLite paths now deployment-configurable, defaulting to their exact pre-Block-1 local paths when unset |
| `config.cors_allowed_origins()` (ADR-061) | NEW — replaces hardcoded `allow_origins=["*"]`; demo mode unchanged, beta/production mode defaults to an empty allowlist unless explicitly configured. Cannot affect the mobile app (CORS is browser-only) |
| `NotificationStore` (`backend/app/notification_store.py`, ADR-061) | NEW — durable SQLite backing for registered push tokens + dispatch/review dedup, gated on the existing `STRATUS_PERSIST_MEMORY` flag. A real bug found and fixed while wiring this in: the background poller's empty-token short-circuit ran before the store's lazy hydration, which would have silently defeated the persistence on the very first cycle after a real restart |
| STRATUS Watch fatigue/cooldown persistence | EVALUATED, NOT PERSISTED (ADR-061 Decision 4) — a deliberate, documented beta-stage gap, bounded by the independent per-event dispatch dedup which prevents an actual duplicate push regardless |
| `resolveApiBaseUrl()`/`isLanOrLocalUrl()` (`mobile/constants/config.ts`, ADR-061) | NEW — `EXPO_PUBLIC_APP_ENV` (`development`/`preview`/`production`, set per `eas.json` build profile) gates the invariant: a preview/production build throws at load time rather than silently falling back to a LAN address when `EXPO_PUBLIC_API_BASE_URL` is missing, LAN-shaped, or not HTTPS |
| `config.startup_config_summary()` (ADR-061) | NEW — one non-secret line logged at process startup stating effective runtime mode/tickers/persistence/CORS |
| Test growth | `backend` 267 → 284 (+17: `test_deployment_config.py` 11, `test_notification_persistence.py` 6); `test_beta_hardening.py`'s restart-safety-matrix test updated to assert the new persisted-token behavior. `logan_core` unaffected (306). mobile Jest 100 → 125 (+25: `lib/__tests__/config.test.ts`) |

See docs/DECISIONS.md ADR-061 for the full decision record, including the exact reasoning behind what was
and was not persisted, and the remaining owner-facing Fly.io steps.

---

*Update this document at every session end.*
*Logan Intelligence Current Implementation State — v3.1.2 | 2026-08-03*
*v3.1.2 changes: TriggerEvent registry row added to backend table. Culture and Personal Finance receptors noted. 7 domains count updated. Reduced-motion mode added to mobile table. Sprint 2A target state updated to include TriggerEvent, World Model trigger_events, and all three Checkpoints.*
*v3.1.3 changes: Entire document rewritten from a direct repository inspection (git remote, logan_core/ and mobile/app/ directory listings) rather than left as an unverified placeholder — see the note at the top of this file. Repository, Mobile App, Backend, Infrastructure, "What Exists", and "What Is Mocked vs. Real" sections replaced with verified current state. Sprint 2A Target State section below is unchanged (it describes a future target, not current state).*
*v3.1.3 code-foundation pass (2026-08-05): Backend table updated for the actual code changes — MemoryRecord.user_id, watch/remind interaction types, EvidenceTrust/ConclusionConfidence model-version reservations, OutcomeRecord v2, SourceObservation, and the process_outcome() stub, all BUILT this session (40 tests passing, up from 28). Added a "Known ADR-034 conflict, not fixed" note documenting two pre-existing priority_score/community_momentum issues found during required inspection but left unmodified as outside this pass's authorized scope. See V3.1.3_IMPLEMENTATION_SUMMARY.md for the full session record.*
*V3.1.4 changes (BATCH-1/2/3): ADR-034 conflict fixed (community_momentum removed from ranking); priority_score renamed to internal_rank_score and made genuinely internal-only (ADR-029); ActiveContext.user_id added; decision_trace populated across all layers; contradiction path documented as reserved/unreachable rather than silently dead; fatigue/cooldown windows wired into real expiration logic; domain fields typed as the `Domain` Literal throughout; full per-layer unit test coverage added (95 logan_core + 8 backend tests, up from 40); Black/Ruff/mypy and ESLint/Prettier tooling added with CI; TriggerEvent and OpportunityLifecycle/Decay Engine content relabeled SPECIFIED — NOT IMPLEMENTED across the doc set (OD-009).*
*V3.1.4 changes (BATCH-4/5, 2026-08-07): GET /v1/opportunities shipped as a real thin adapter over logan_core (backend/app/opportunities.py), replacing the old static-fixture route; /v1/demo/tesla and /v1/demo/feed marked deprecated, kept functional. Mobile home screen migrated to the real API; Opportunity Card components consolidated into one canonical DeliveredItem-based component; hardcoded LAN IP replaced with EXPO_PUBLIC_API_BASE_URL. Reduced motion (useReducedMotion) wired into every ambient animation across AttentionField/Vessel/LoganCore/OpportunityNode and the Atmosphere layer; accessibility labels/roles/hints/states added across interactive elements reached this pass; centralized fetch (lib/apiClient.ts) added with timeout/retry/cancellation and five distinct home-screen states (loading/loaded/empty/timeout/error); this repository's first Jest/RNTL test suite added (19 tests) and wired into CI; design tokens adopted on the flagship screen and Vessel; AttentionAtmosphere.tsx integrates a capped, feed-connected subordinate Atmosphere layer into the live screen, resolving ADR-028's open item. backend test count: 15 (up from 8). mobile test count: 19 (new). See V3.1.4_IMPLEMENTATION_SUMMARY.md for the full batch-by-batch record, including mobile deployment (Apple-signed iOS build) status.*
*Sprint 3.6.6 changes (2026-08-14): first real vertical slice, narrowly scoped -- NVIDIA earnings data -> deterministic STOCK_EARNINGS_BEAT detection -> existing unmodified intelligence pipeline -> delivered opportunity. New: minimal `TriggerEvent` contract (contracts/trigger.py, not the full TRIGGER_EVENT_FRAMEWORK.md shape), `trigger_detection/stocks.py` (one trigger code only), `receptors/providers/` (EarningsProvider Protocol + FixtureEarningsProvider, explicitly labeled non-live), `receptors/stocks_earnings.py`. Small additive extensions (all backward-compatible, default-off/zero): Orchestrator's opt-in `trigger_detector` dependency, EnrichedEvent.trigger_events, EvidenceTrust.trigger_confidence_bonus, ConclusionConfidenceEngine's formula (+bonus, still deterministic -- not the ADR-032 ML surface, not ADR-015's Mental Model exclusion). backend/app/logan_feed.py and /v1/opportunities were NOT wired to this -- the live demo is unaffected, NVDA included. logan_core test count: 126 (up from 95). See docs/DECISIONS.md ADR-042, which partially supersedes OD-009 (only STOCK_EARNINGS_BEAT moved from SPECIFIED to BUILT -- the rest of the TriggerEvent framework remains SPECIFIED — NOT IMPLEMENTED). Next step: a real stocks-earnings provider + credentials, an explicit decision not made this sprint.*
*Sprint 3.6.6B changes (2026-08-14): first live provider -- `FmpEarningsProvider` (receptors/providers/fmp.py), backed by Financial Modeling Prep, satisfying the existing EarningsProvider Protocol unmodified. `httpx>=0.27` added as a genuine new logan_core runtime dependency (explicitly flagged, not silent). 17 contract tests, all httpx.MockTransport-mocked, no real network calls in the normal suite. A separate, never-pytest-collected live-verification script (logan_core/live_verification/nvda_earnings.py) added. Live-verified same day: found and fixed a genuine selection-logic bug (fetch_latest_earnings was picking the latest-dated entry regardless of whether it was reported yet, occasionally selecting an upcoming scheduled report over the latest actually-reported one) -- fixed to prefer reported entries, falling back to scheduled only when nothing has been reported. 2 regression tests added (19 total in that file). logan_core test count: 143. Not wired into backend/app/logan_feed.py or /v1/opportunities yet. See docs/DECISIONS.md ADR-043.*
*Sprint 3.6.6C changes (2026-08-15): wired the proven live NVDA path into GET /v1/opportunities, config-gated and off by default (STRATUS_LIVE_NVDA_EARNINGS, backend/app/config.py). backend/app/logan_feed.py: FmpEarningsProvider fetches the latest NVDA report when enabled; substitution only happens when the report actually satisfies STOCK_EARNINGS_BEAT (pre-checked via the same evaluate_earnings_beat_condition StocksTriggerEvaluator uses internally) -- a valid-but-non-beating report, provider failure, missing API key, or no data all fall back to the simulated NVDA fixture, never both, never mislabeled. Orchestrator's trigger_detector is only constructed when the flag is on, keeping disabled-mode's ExecutionTrace byte-for-byte identical to every pre-3.6.6C caller. Both of these behaviors were corrected during implementation review (an earlier unconditional-wiring draft would have changed the trace for every entity even with the flag off, and would have surfaced a real-but-non-firing report as a live opportunity) -- see ADR-044. Also found and fixed a cross-suite test-isolation bug exposed by config.py's import-time load_dotenv(): a logan_core FMP test that assumed no FMP_API_KEY in the environment now explicitly isolates via monkeypatch, matching its sibling test's existing pattern. 13 new backend tests added (31 total, up from 18); logan_core test count unchanged at 143 (one existing test hardened, none weakened). Live-verified locally against the real FMP API through the actual GET /v1/opportunities route: real EPS 1.87 vs. consensus 1.76 (beat_pct 6.25%), STOCK_EARNINGS_BEAT fired, confidence_score 0.595, exactly one NVDA item returned, all 10 other simulated entities untouched, no internal_rank_score/secret leak. Stopped here per the sprint's explicit boundary instruction -- no new trigger codes added. See docs/DECISIONS.md ADR-044.*
*Sprint 3.6.6D changes (2026-08-18): two more stocks trigger codes plus a ConvergenceDetector foundation, per an explicit owner confidence-model decision (fixed registry constants only, never invent weights; strongest-trigger-wins on convergence, never summed; no ML/calibration redesign yet). New: STOCK_EARNINGS_MISS (logan_core/trigger_detection/stocks.py -- same architecture as STOCK_EARNINGS_BEAT, +0.20 per the registry) and STOCK_EARNINGS_IN_LINE (0.0 contribution, "closes hypotheses" per the registry, evidence still preserved); both reuse the existing actual_eps/consensus_eps provider fields, zero FmpEarningsProvider changes. Inspected and rejected STOCK_EARNINGS_QUALITY_WARNING, STOCK_GUIDANCE_RAISED/LOWERED, and five other registered stocks codes for this slot -- each needs data FMP's earnings endpoint doesn't return (one-time-item flags, prior guidance, options flow, price data, filings, analyst ratings); documented in ADR-045 rather than silently skipped. New logan_core/convergence/ package: ConvergenceDetector.resolve() picks the single strongest TriggerEvent.confidence_contribution among everything attached to one EnrichedEvent instead of summing (wired into EvidenceTrustEngine, replacing its prior sum(...) computation) -- explicitly NOT the canonical Hit-Detection-layer Convergence Detector (STOCK_CONVERGENCE_MULTI_SOURCE, OpportunityEvidence output), which remains entirely unbuilt; World Model's (entity_id, signal_type) dedup key was not widened, a separate larger decision left undecided. 19 new logan_core tests (162 total, up from 143); one pre-existing test rewritten (not weakened) since its old input now correctly fires the new IN_LINE trigger. Live-verified the existing FMP /v1/opportunities NVDA path still returns the identical result after this refactor (EPS 1.87 vs. 1.76, STOCK_EARNINGS_BEAT fired, confidence 0.595). MISS/IN_LINE proven at the fixture/integration level only, not yet against a live report that actually misses or lands in-line -- not forced, per the standing "don't fabricate a fire" rule. See docs/DECISIONS.md ADR-045.*
*Sprint 3.6.6 close-out changes (2026-08-19 through 2026-08-21) -- Sprint 3.6.6 now functionally complete. Sprint 3.6.6E fixed repeated-polling confidence inflation and overclaimed-personalization wording. Sprint 3.6.6F + companion mobile work shipped STRATUS Watch's first real push notifications (Expo token registration/tap-to-open, backend dispatch reusing the existing interruption=="alert" gate). Sprint 3.6.6G fixed badge/push state coherence (foreground-refresh root cause); 3.6.6H cleaned up push wording. Sprint 3.6.6I (ADR-046) fixed a same-timestamp engagement-fixture artifact that had been inflating lifecycle_state for 10 of 11 simulated entities -- the same trace run that first surfaced FED alerting via a fully generic, unconnected personal_relevance=0.50 default, flagged then as an open question. ADR-047 wired real behavioral interaction capture (card-open/dwell/notification-tap) through the existing, previously-call-less Orchestrator.run_feedback_loop() end-to-end into MemoryStore via a new POST /v1/interactions route. ADR-048 closed the loop: process-lifetime persistent UserModel (_get_user_model(), mirroring _get_orchestrator()'s singleton pattern), repeated behavioral evidence folding into established_behaviors/domain_preferences/inferred Interest (MIN_REPEAT_EVIDENCE=2, confidence values reused verbatim from FeedbackEngine, inferred_expertise/explicit data always preserved), source-aware relevance (ReasoningResult.connected_entities_explicit/_inferred, explicit keeps the 0.6 relevance bump, inferred-only bounded to 0.5), and restored Orchestrator ownership (run_feedback_loop()'s content parameter now also accepts a callable). ADR-049 -- the sprint's final Watch-policy decision record -- replaced PolicyEngine's single urgency>=0.7 alert gate with explicit Personal and Exceptional eligibility routes, every threshold reused from an existing anchor (no new numeric policy invented); PolicyEngine remains the sole communication gate, PrioritizationEngine's fatigue/cooldown/rank-score logic is unchanged and retains final authority over the actual interruption value. logan_core test count: 197 (up from 162). backend test count: 82 (up from 31). mypy/ruff/black clean throughout. Known limitations carried into Sprint 3.6.7 (not resolved this sprint): Personal-route-qualifying items can still land as digest due to Prioritization's separate rank-score gate; impression/exposure semantics (server-surfaced vs. client-rendered) unresolved; Personal/Exceptional routes share identical fatigue/cooldown handling; Exceptional's thresholds validated only against simulated fixtures; UserModel persistence remains process-lifetime only; MemoryStore.query() remains single-user-only; internal_rank_score>=0.6 is a soft coupling between Policy and Prioritization; FIELD BIAS learning, Ask STRATUS linkage, broader trigger expansion, and broader ML calibration remain deferred. See docs/DECISIONS.md ADR-046/047/048/049 and SESSION_NOTES.md for the full decision records and the Sprint 3.6.7 starting-point section.*
*Sprint 3.6.7 Block 1 changes (2026-08-21): two ordered deliverables on branch feat/sprint-3.6.7-stock-signal-expansion, cut from the clean Sprint 3.6.6 integration HEAD. (1) ADR-050 resolved the exact Sprint 3.6.7 starting objective ADR-049 flagged: PrioritizationEngine.prioritize() now computes visibility (rank-score-driven, unchanged) and interruption (PolicyEngine's communication_mode-driven, decoupled from the rank-score tier) independently, past the fatigue/cooldown/permitted vetoes -- all three unchanged and still fully authoritative. A Personal-route item can no longer silently land as digest purely because internal_rank_score sits in [0.35, 0.6); high rank alone still never forces an alert without a genuine Policy route decision (verified by the converse test). (2) ADR-051 generalized the earnings-only trigger architecture across two new signal types sharing the identical Provider/Receptor/Evaluator pattern: STOCK_PRICE_MOVE_SIGNIFICANT (FMP /quote, +0.10 confidence) and STOCK_ANALYST_UPGRADE/DOWNGRADE (FMP /grades, +0.08 each, trusting the provider's own action classification rather than inferring direction from rating text) -- both already fully specified in TRIGGER_REGISTRY_STOCKS.md, no new confidence numbers invented. New: receptors/providers/base.py's Quote/QuoteProvider/GradeChange/AnalystGradesProvider, fmp.py's FmpMarketDataProvider (a sibling to FmpEarningsProvider, not a merge), fixture.py's FixtureMarketDataProvider, receptors/stocks_market_data.py, and StocksTriggerEvaluator's signal_type dispatch (_evaluate_earnings extracted unchanged, _evaluate_price_move/_evaluate_analyst_grade added). Live-verified against real FMP data 2026-08-21 (honest no-fire result on both: NVDA's real change_pct was -0.98%, most recent real grade action was "maintain") -- see logan_core/live_verification/nvda_market_data.py. A real, fixture-driven, full-pipeline alert was proven deterministically in test_pipeline_market_data.py (NVDA holding + qualifying price move -> communication_mode="alert", watch_route=personal, interruption="alert", exercising ADR-049/050 together). Deliberately deferred: wiring either new signal into backend/app/logan_feed.py's live /v1/opportunities path -- WorldModel.process()'s (entity_id, signal_type) dedup key would silently drop one of two simultaneous live signals for the same entity, which is Sprint 3.6.7 Block 2's own scope (signal convergence) to resolve first. Also deferred, with no reliable current provider data: STOCK_GUIDANCE_RAISED/LOWERED, STOCK_OPTIONS_FLOW_SURGE, and "unusual volume"/"volatility spike" (neither has a registered trigger code at all, so implementing either would mean inventing an unbacked confidence_contribution -- not done). logan_core test count: 197 -> 248 (+51: 5 for ADR-050, 46 for ADR-051). backend test count unchanged at 82 (no backend/ files touched this block). mypy/ruff/black clean. See docs/DECISIONS.md ADR-050/051 and SESSION_NOTES.md for the full decision records and the Sprint 3.6.7 Block 2 (signal convergence) starting-point recommendation.*
*Sprint 3.6.7 Block 2 changes (2026-08-22): resumed after a PC crash interrupted the first attempt at the reconnaissance stage (verified clean restart from 6fe4fdd, no partial work existed). Implemented STOCK_CONVERGENCE_MULTI_SOURCE (moved from SPECIFIED -- NOT IMPLEMENTED to BUILT) via the owner-selected "Option 1: parallel convergence tracker" approach, explicitly rejecting a widened WorldModel dedup key. New logan_core/convergence/tracker.py's StockConvergenceTracker: a persistent, process-lifetime component that watches TriggerEvents trigger detection already produces and fires when >=3 distinct signal_types converge on one entity within a 30-minute window -- windowed on detected_timestamp (real evaluation-time "now"), not event_timestamp/captured_at, since an earnings report's date, a quote's real-time timestamp, and an analyst action's date are independently sourced and routinely diverge by far more than 30 minutes even when all three are detected as live opportunities in the same poll. An active episode (the same qualifying combination observed again before it ages out) reuses its trigger_id/event_timestamp rather than minting a new alert every poll; distinct source types are tracked as a set, so repeated polling of one already-observed signal_type can never manufacture a false third source. WorldModel's own (entity_id, signal_type) dedup/corroboration semantics are completely unmodified -- verified by the full pre-existing test_world_model.py suite passing unchanged. Separately, fixed the real "silently drops all but one" bug ADR-051 finding 5 identified: new _collapse_duplicate_event_ids/_merge_entity_events/_attach_trigger helpers in orchestrator/pipeline.py, called from Orchestrator.run(), now merge multiple distinct-signal_type EnrichedEvents for one entity into one coherent opportunity instead of only the last-processed raw_signal surviving -- a deliberate no-op whenever only one distinct event exists, so every pre-Block-2 caller/test is byte-for-byte unaffected. backend/app/logan_feed.py now wires Block 1's live price-move/analyst-grade signals into /v1/opportunities alongside earnings (same STRATUS_LIVE_NVDA_EARNINGS flag, each additively gated on its own trigger actually firing, never a fabricated non-event or fabricated convergence). New tests: test_convergence_tracker.py (11, tracker unit coverage -- fire condition, window boundary, duplicate/repeated-polling suppression, repeated-active-episode suppression, provenance, decision trace), test_pipeline_convergence.py (6, coherent-merge + end-to-end convergence firing through the full unmodified downstream pipeline), test_live_nvda_market_data.py (7, live wiring/gating/failure-isolation/API contract). One unrelated pre-existing flake found and fixed while running the full suite (test_pipeline_market_data.py's price-move test decaying below its own rank-score assertion purely from real-world time passing since its fixture's fixed timestamp -- confirmed via git stash against clean 6fe4fdd before touching it). logan_core test count: 248 -> 265 (+17). backend test count: 82 -> 89 (+7). mypy/ruff/black clean. No merge to main. See docs/DECISIONS.md ADR-052 for the full decision record.*
*Sprint 3.6.7 Block 3 changes (2026-08-22): closed the loop ADR-047/048 explicitly left open. Persistence authority decided before implementation (per explicit confirmation): a new, dedicated local SQLite store (backend/data/stratus_state.db, config-gated off by default via STRATUS_PERSIST_MEMORY), not an extension of the historical memory_engine.py/logan_memory.db. MemoryStore (logan_core/memory/store.py) gains an optional db_path -- None (every pre-Block-3 caller/test) is byte-for-byte unchanged; a real path adds schema-versioned SQLite persistence with bounded-history compaction (2000 prunable records/user). UserModel is never stored directly -- always rebuilt from persisted records via the existing UserModelBuilder.build() pattern. Exposure/impression semantics: InteractionType gains "impression" (a deterministic system fact, never interpreted by FeedbackEngine, routed through a new Orchestrator.run_exposure_loop() -> LearningEngine.process_exposure()) and "ask_followup" (genuine engagement, 0.80 confidence); RecordType gains "exposure_record", structurally excluded from UserModelBuilder's existing behavioral-evidence folding so impressions alone can never manufacture relevance. Idempotency: one lifetime exposure_record per (user, event); a new 5-minute dedup window on process_feedback for (user, event, interaction_type) absorbs duplicate/retry fires without suppressing genuinely later sessions. Deterministic behavioral relevance model (user_model/model.py, no LLM involved): maturity scaling (+0.02/qualifying event beyond MIN_REPEAT_EVIDENCE, capped +0.10, weight capped 0.90), recency-based decay (14-day half-life measured from each pair's most recent qualifying evidence, pruned below 0.10), exposure-fatigue dampening (weak, bounded -0.05, only on an existing inferred interest, only after a real recency gap since last engagement, never manufactured from mere non-engagement). ReasoningResult gains inferred_relevance_strength (the matched inferred Interest's weight); OpportunityEngine's "connect" step replaces the old flat 0.5 inferred-connection floor with a maturity-scaled [0.5, 0.59] range via _scale_inferred_relevance(), strictly below the explicit tier's 0.6 bump (ADR-048's invariant unchanged, verified against the exact pre-existing test assertions before implementing). PolicyEngine/PrioritizationEngine (ADR-049/050) untouched -- mature evidence now measurably moves events within existing, unmodified Watch thresholds. Mobile: useImpressionTracking.ts fires one impression per AttentionField focus change, reusing existing focusedId state rather than new viewport-tracking UI; ask_followup is a full backend/domain contract but deliberately not wired to the disconnected legacy /v1/ask chat stub (real Ask-STRATUS-about-this-opportunity linkage remains its own deferred item). Acceptance scenario proven end-to-end and covered by an automated test: no explicit AMD holding, 4 recorded engagements, a simulated backend restart, and the inferred AMD relevance is still present afterward, genuinely matured past a single interaction's own confidence. Companion live-convergence-verification carryover from Block 2 (ADR-054): new logan_core/live_verification/nvda_convergence.py, run live against real FMP data -- only 1 of 3 NVDA signal types fired today (earnings beat), so STOCK_CONVERGENCE_MULTI_SOURCE honestly did not qualify (not forced), while the full pipeline still produced a real alert from the earnings signal alone. logan_core/backend test count 354 -> 405 (+51). mobile test count 88 -> 94 (+6). mypy/ruff/black clean; tsc --noEmit/eslint clean. No merge to main. See docs/DECISIONS.md ADR-053/054 for the full decision records.*
*Sprint 3.6.7 Block 4 changes (2026-08-22): wired Block 3's ask_followup concept into a real, grounded, per-opportunity Ask STRATUS flow. Confirmed by inspection before writing any code: no LLM exists anywhere in this codebase -- the pre-existing generic /v1/ask route (backend/app/main.py) is itself a static template over the historical memory_engine, not a model call; adding one would be a new dependency requiring separate explicit confirmation, not made here. Instead built entirely deterministic: new OpportunityContext/OpportunityContextCache (backend/app/ask_context.py) snapshot the same PipelineResult that already produces each FeedItem (real DeliveredItem narrative fields, ConclusionConfidence classification/limiting_factors/alternatives, attached TriggerEvents, Dimensions.personal_relevance -- never internal_rank_score, ADR-029), refreshed every _run_feed_pipeline() call. AskRequest/AskResponse (models.py) gain additive optional event_id/session_id/grounded fields on the existing /v1/ask route -- one route, not a second endpoint; the pre-existing generic path is unaffected and, for the first time, actually test-covered. answer_question() (ask_engine.py) deterministically classifies the question (what changed, why now, which signals, convergence, confidence, what would weaken this, personal relevance, comparison) against real fields via ordered keyword matching, falling back to an honest real-data overview rather than fabricating when nothing matches; an unresolvable event_id (stale/restarted) gets an honest "no current context" answer, never invented content. A successful contextual question records a real ASK_FOLLOWUP interaction through the existing Block 3 record_interaction()/FeedbackEngine path (0.80 confidence tier, unchanged) -- invalid opportunity, empty message, and generic (no-context) questions never record engagement. New bounded, process-lifetime session store (backend/app/logan_feed.py, _ASK_SESSION_LIMIT=500) holds only the structural continuity anchor (which event_id a session is discussing, which event_ids it already recorded ASK_FOLLOWUP for) -- never raw transcript text -- deliberately not persisted to SQLite (short-lived UI convenience, not durable preference data, a documented persistence-boundary choice within the block's own allowed scope). Feedback-loop protection: at most one ASK_FOLLOWUP contribution per (session, opportunity), verified to sit correctly on top of (not conflict with) Block 3's pre-existing session-agnostic 5-minute short-window dedup. Mobile: Vessel.tsx gains one "Ask STRATUS about this" button per opportunity card; ask.tsx reads eventId/entityId/displayName/domain via Expo Router params (a new pattern for this app) and swaps in contextual first-state copy/starter prompts plus an honest "Discussing {entity}" chip shown only when real context exists; the pre-existing generic drawer-menu entry point is unaffected. Acceptance scenario proven end-to-end and covered by an automated test: a user opens an opportunity with no explicit holding (AAPL stands in for the task description's "AMD," which is not a real fixture entity in this codebase), asks real contextual follow-ups across several independent sessions, a simulated backend restart occurs, and the inferred relevance is still present afterward, genuinely matured past a single ask_followup's own confidence. logan_core/backend test count 405 -> 446 (+41). mobile test count 94 -> 100 (+6). mypy/ruff/black clean; tsc --noEmit/eslint clean. No merge to main. See docs/DECISIONS.md ADR-055 for the full decision record.*
*Sprint 3.6.8 Block 2 changes (2026-08-22): removed founder/single-user assumptions from the stateful STRATUS intelligence loop. Recon before writing any code found most of logan_core was already multi-user-safe by construction -- Orchestrator.run()/run_feedback_loop()/run_exposure_loop() already take an explicit user_id, MemoryRecord.user_id is required per ADR-033, PrioritizationEngine's AttentionState was already a per-user dict internally -- the real gap was concentrated in backend/app/, which never threaded any real per-request identity through (8 hardcoded LOCAL_FOUNDER_USER_ID call sites), plus one genuine pre-existing bug inside logan_core itself: orchestrator/pipeline.py's run() called memory_store.query(entities=...) with no user_id filter at all, leaking every user's feedback_records for a shared entity into every other user's UserModel rebuild. Two review points were resolved before continuing, not assumed: (1) record_interaction() ownership -- confirmed by direct inspection and a repo-wide grep that it has always gone through Orchestrator.run_feedback_loop()/run_exposure_loop() (ADR-047's content-builder-callable pattern), no bypass to feedback_engine.interpret()/learning_engine.process_feedback() exists anywhere in this codebase; no code change needed. (2) DomainPref(weight=0.5) -- confirmed it is a required contract field with no documented semantic meaning and, checked directly, no consumer anywhere in Reasoning/OpportunityEngine/Policy/Prioritization reads domain_preferences[].weight, and it is never updated again after creation; left the value unchanged (removing it would require a contract change, out of scope) and added explicit inline comments at both construction sites (user_model/model.py) documenting why it's inert rather than inventing new weighting semantics or silently leaving it unexplained. Decision (owner-approved): new X-Stratus-User-Id request header (backend/app/user_context.py's resolve_user_id(), a FastAPI dependency, not authentication) -- absent defaults to LOCAL_FOUNDER_USER_ID so every pre-Block-2 caller is unaffected; wired into every user-facing route. MemoryStore.query()/.all() (logan_core/memory/store.py) now require user_id explicitly -- no default, no "all users" mode -- closing the real leak at its root (orchestrator/pipeline.py's run() now passes user_id=user_id) with no SQLite schema change (the column/index already existed from Sprint 3.6.7 Block 3); ~15 test call sites plus 3 production call sites migrated deliberately. backend/app/logan_feed.py's process-lifetime singletons converted to per-user dicts: _user_model -> _user_models, the OpportunityContextCache -> per-user (closing the sharpest read-side leak, since OpportunityContext carries personalized personal_relevance/connection_basis/is_new_for_user), _ask_sessions -> keyed by (user_id, session_id) since a session_id is client-generated and not itself a secret. The shared _orchestrator (one World Model) is deliberately not duplicated per user -- two users seeing the identical event_id for the identical real-world fact is correct; only the personalization layer (UserModel, AttentionState, OpportunityContext/session state) is user-scoped. New-user seeding is genuinely blank (owner-approved) -- only LOCAL_FOUNDER_USER_ID gets the existing NVDA holding/AI_SECTOR interest; every other user_id gets UserModelBuilder.seed()'s own blank/unknown defaults, never a copy of the founder's portfolio. backend/app/notifications.py's registered-token/dispatched/reviewed-event-id state converted to per-user_id dicts; the background poller now loops once per registered user, computing that user's own alert-eligible items and dispatching only to their own tokens (a real behavior change -- the poller now runs the full pipeline once per registered user per cycle -- an accepted, flagged cost at current local-dev scale, not a blocker). New tests: test_multi_user_isolation.py (14 -- identity-boundary backward compatibility, two distinct user_ids getting independent UserModels while sharing the same World Model event identity, behavioral-evidence isolation, explicit-vs-inferred relevance isolation including the founder-seed-never-copied guarantee, Ask STRATUS session/OpportunityContext isolation including a deliberate session-id-collision case, ASK_FOLLOWUP recorded under the correct user, Watch notification-review isolation, push-token/dispatch/review isolation, and restart-persistence staying correctly user-scoped). Every pre-existing test across both logan_core and backend was migrated to the new required-user_id signatures, including test_compaction_is_scoped_per_user (logan_core/tests/test_memory_store_persistence.py), rewritten to check each user's own .all(user_id=...) view directly now that a global "all users" read no longer exists. backend test count: 179 -> 193 (+14); logan_core unchanged at 306. Combined 485 -> 499. mypy/ruff/black clean -- the same 14 pre-existing **dict[str, object] mypy findings from Block 1 are unchanged in count and location. AskRequest/AskResponse and every other request contract are unchanged (identity travels via header, not body) -- no mobile contract impact, no mobile test re-run required. Deferred/flagged: mobile has no persisted per-device identity mechanism yet, so X-Stratus-User-Id is never actually sent by any real client today -- wiring that up needs a new client-side storage dependency, a separate decision; push-token/dispatch/review state remains in-memory, process-lifetime only per user; the single process-wide _state_lock remains coarse-grained across every user's pipeline runs; docs/specs/.../27_SECURITY_PRIVACY_COMPLIANCE.md's prior "multi-user persistence explicitly excluded from V3.1.4 scope" note needs a follow-up edit. No merge to main; commit not pushed pending review. See docs/DECISIONS.md ADR-057 for the full decision record.*
*Sprint 3.6.8 Block 3 changes (2026-08-23): turned the existing single-turn grounded Ask STRATUS path into bounded, genuinely conversational reasoning while keeping deterministic STRATUS intelligence as the sole source of authoritative truth. Recon before writing any code found the mobile client (mobile/app/ask.tsx) already renders and resends a real accumulating multi-turn conversation, already resends the same eventId every turn, and already handles loading/error states -- the entire gap was server-side. Decision: bounded conversation history added to the existing per-(user_id, session_id) _AskSession (Block 2's own store, not a new one) -- _MAX_ASK_HISTORY_TURNS=6 pairs / _MAX_ASK_HISTORY_CHARS=4000 as a secondary bound, both reasoned small integers in this codebase's existing style, not tuned against usage data; eviction always drops a full (user, assistant) pair to preserve the strictly-alternating-role invariant Anthropic's Messages API requires; no durable raw-chat database introduced. New: opportunity-anchor-change detection in set_ask_session_event() -- a genuine event_id change within a session deterministically clears history (never accidentally resolving a follow-up against a different opportunity), while resending the same event_id every turn (mobile's actual behavior) is correctly a no-op. Authoritative-context-wins made an explicit, structurally-enforced invariant: build_system_prompt() still takes only `context`, so history can never be concatenated into the system prompt at all; new prompt text states current context always wins over anything implied by an earlier turn. No-invented-signal-ranking added to the system prompt explicitly (the deterministic path's own _dominant_signal_answer already enforced this). Provider abstraction evolved additively: new vendor-neutral ConversationTurn, AskLlmProvider.generate() gained a defaulted history parameter, only AnthropicAskLlmProvider translates it into Anthropic's own message shape -- FixtureAskLlmProvider.calls grew from 2-tuples to 3-tuples, the one small deliberate breaking change, migrated across 2 existing call sites. Deterministic fallback mid-conversation: reasoned explicitly rather than assumed -- the fallback path does not attempt conversational reference resolution (a materially larger, separate change); whichever path actually produced the answer (LLM or fallback) is what enters history, since both are real and true. ASK_FOLLOWUP bound: verified unchanged, not re-derived -- the existing per-(session, opportunity) cap already made conversational depth independent of personalization strength; new tests prove 12 real turns and a mid-session opportunity switch both stay correctly bounded. Mobile: zero code changes -- the existing screen already supported everything this block needed; full mobile suite (100 tests), tsc --noEmit, and eslint re-run and confirmed clean. New tests: test_ask_conversation.py (30 -- first/second/third-turn continuity, pronoun/reference resolution, bounded turn/char eviction with alternating-role proof, same-session-id-two-users isolation, same-user-two-sessions isolation, opportunity-anchor-change reset and no-op-on-same-anchor, invalid/stale anchor cross-user isolation, authoritative-context-wins and no-invented-ranking system-prompt assertions, first/later/retained-history injection resistance at both the system-prompt and wire-message-shape level, provider failure on first and later turns, deterministic-fallback-answer entering history correctly, malformed-response fallback, LLM-disabled multi-turn behavior, ASK_FOLLOWUP bounded across 12 turns and an opportunity switch, and existing single-turn paths still working). backend test count: 193 -> 223 (+30); logan_core unaffected (306). mypy/ruff/black clean (one real mypy fix: `messages` needed typing as list[anthropic.types.MessageParam], not a plain dict list, for the SDK's overload resolution). No merge to main; commit not pushed pending review. See docs/DECISIONS.md ADR-058 for the full decision record.*
*Sprint 3.6.8 Block 4 changes (2026-08-23): a recon-driven beta-readiness hardening pass across the fully integrated provider/pipeline/mobile/conversational-Ask/behavioral-learning loop, governed mid-session by an explicit owner rule (production opportunity generation must be live-data-first; fixtures belong only in tests/acceptance scenarios; never lower a threshold or fabricate a signal to force a live check to pass). Two real bugs found and fixed: (1) dispatch_eligible_notifications() (backend/app/notifications.py, Block 2) already documented that one user's failure must not stop another's dispatch, but only the push send itself was guarded -- the per-user pipeline call (get_alert_eligible_items) was not, so an exception there would have silently skipped every other registered user for that whole poll cycle; widened the guard to the entire per-user body, new test proves the fix. (2) _notification_poll_loop() (backend/app/main.py) is an async def coroutine that called the fully synchronous dispatch_eligible_notifications() directly -- unlike every HTTP route (already threadpooled by FastAPI/Starlette automatically), this ran a full per-user pipeline (including up to three sequential 10s-bounded live FMP calls when enabled) directly on the main event loop, which would have frozen every other concurrent request during a slow poll cycle; fixed with asyncio.to_thread(), currently inert since the live flag defaults off but a real latent hazard. Defensive hardening: logan_core/receptors/providers/fmp.py gained a _redact() helper stripping the real API key from any error message built from raw FMP response text, satisfying "never log API keys" by construction rather than by observed absence of the problem. Minimum viable observability added: one log line per successfully-grounded Ask STRATUS turn (user_id, entity_id, deterministic-vs-llm:<model>) -- routing metadata only, never question/answer text, using the existing print(f"[tag] ...") convention already established throughout this codebase rather than a new logging framework or vendor. New tests: a restart-safety matrix made explicit and executable (MemoryStore records survive a persisted restart; AttentionState/Ask conversation history/notification-token state do not, regardless of the persistence flag); multi-user regression re-proven through Block 3's actual conversational stack rather than just the original single-turn surface; a full integrated acceptance path (no explicit holding -> real opportunity -> limited relevance -> conversational Ask exchange -> bounded ASK_FOLLOWUP -> real persisted restart -> correctly-scoped survival -> matured-but-bounded relevance -> second user unaffected), fixtures throughout, no live threshold ever touched to force the result. Live-data-first inventory (reported, not fixed, per the owner's explicit instruction not to add new providers without approval): exactly one production call site of simulated_fixtures() exists (backend/app/logan_feed.py's _run_feed_pipeline(), backing the real /v1/opportunities endpoint) -- 10 of 11 hardcoded entities across 6 domains are always simulated; only NVDA has any live-provider path at all (FmpEarningsProvider/FmpMarketDataProvider), itself off by default (STRATUS_LIVE_NVDA_EARNINGS). A genuine nuance found and documented: TSLA/AAPL are real tickers the existing FMP integration could plausibly serve without a new vendor (the live wiring is hardcoded to the symbol "NVDA" specifically, not parameterized by entity); MARKETS/OIL represent aggregate/commodity concepts needing a product decision about what real instrument to track; the remaining five entities (BTC/FED/NFL/MUSIC/POLY) span five different domains with zero existing provider integration, each requiring a genuine new external vendor decision -- none selected, none added. Mobile/EAS readiness recon (reported, not fixed): mobile/eas.json declares build profiles with no EXPO_PUBLIC_API_BASE_URL configured for any of them; constants/config.ts's documented local-IP fallback would silently ship into a real EAS preview/production build (which runs on Expo's remote servers, never reading the gitignored local .env), making the app unreachable for any real tester -- blocked on ADR-006's own open hosting decision, not fixed with a guessed value, matching the same discipline as not attempting Apple signing/credentials. docs/specs/.../27_SECURITY_PRIVACY_COMPLIANCE.md corrected precisely per the owner's explicit instruction: the stale "no way for a second distinct user to exist" claim replaced with an accurate description of real backend state isolation (Block 2, MemoryStore/UserModel/AttentionState/OpportunityContext-cache/Ask-session state all genuinely scoped by user_id) explicitly distinguished from authentication, which still does not exist -- the X-Stratus-User-Id header is never verified against anything, so any process reaching the backend can claim to be any user_id. backend test count: 223 -> 227 (+4); logan_core unchanged (306, only the redaction helper touched, behavior-neutral). Combined 529 -> 533. mypy/ruff/black clean (same pre-existing baseline pattern, unchanged in count/location). Mobile: 100/100 Jest, tsc --noEmit, eslint all clean with zero code changes. No merge to main; commit not pushed pending review. See docs/DECISIONS.md ADR-059 for the full decision record.*
*Sprint 3.6.8 Block 5 changes (2026-08-23): removed the NVDA-only limitation from the already-approved FMP provider architecture, establishing one reusable live equities runtime path for any configured ticker plus an explicit production-vs-demo runtime boundary, governed by the owner's live-data-first rule. Recon before writing any code found the NVDA coupling was entirely a backend/app/ wiring artifact -- logan_core's entire receptor-mapping layer, FmpEarningsProvider/FmpMarketDataProvider, StocksTriggerEvaluator, and StockConvergenceTracker were already fully entity-generic (a repo-wide grep found zero hardcoded NVDA logic in logan_core/); StockConvergenceTracker in particular was already keyed internally by entity_id, so multi-stock convergence entity isolation required no code change, only a proving test. New config.live_stock_tickers() (STRATUS_LIVE_STOCK_TICKERS, comma-separated, deterministic parsing/validation, no network call) falls back to the original STRATUS_LIVE_NVDA_EARNINGS flag exactly when unset -- every pre-Block-5 deployment/test unaffected, verified by the full pre-existing suite passing with zero modifications. The three NVDA-hardcoded functions (backend/app/logan_feed.py) generalized to accept any ticker, exact same failure-mode discipline, deliberately preserving pre-Block-5 substitution semantics (only STOCK_EARNINGS_BEAT triggers substitution, not the also-real STOCK_EARNINGS_MISS/IN_LINE -- a real, now live-verified gap, not a hypothetical one, left as a deliberate, separate decision). Two real bugs found and fixed while generalizing the wiring, both instances of the exact "simulated intelligence silently entering a live-labeled opportunity" pattern the owner's rule forbids: (1) TSLA's simulated corroborating signal was previously appended unconditionally regardless of whether TSLA's own primary signal was genuinely live that poll -- a real live TSLA earnings beat could have been silently joined by a fabricated corroborating signal; fixed via a live_substituted set tracking exactly which tickers went live. (2) Price-move/analyst-grade fetches were previously gated only on the flag being on, independent of whether earnings itself went live that poll -- a live price-move signal could have been spliced onto a simulated primary earnings signal, since World Model's narrative is driven by the first/primary signal in the list; fixed with the same live_substituted gate, so an opportunity is now always fully live or fully simulated, never blended. New config.live_data_only_mode() (STRATUS_RUNTIME_MODE=live/beta/production) is the production-vs-demo runtime boundary -- in that mode fixtures starts empty rather than the full simulated 11-entity set, so an entity only appears if a genuine live fetch substituted it; unsupported domains and non-qualifying configured tickers are both honestly absent, never simulated-substituted, while demo mode's existing fixture-rich behavior remains completely unchanged. Data provenance requirement satisfied without building anything new: the existing source_id field (RawSignal/TriggerEvent, "fmp" vs. simulated source IDs) and the existing print-based [live-stocks] log convention already serve as the live/fixture audit trail end to end. New logan_core/live_verification/live_equities.py generalizes the existing per-ticker verification scripts into one accepting any ticker list; run for real against the real FMP API 2026-08-23: NVDA earnings beat fired (1.87 vs. 1.76 consensus); TSLA earnings miss fired (0.33 vs. 0.50) and correctly did not go live through the real _run_feed_pipeline() wiring, directly confirming the deliberate BEAT-only gap with real data; AAPL earnings beat (2.02 vs. 1.89) and a real analyst downgrade both fired, combining into one coherent live opportunity; no convergence fired for any ticker (none reached 3 distinct signal types), an honest result, not forced. New tests: test_config_live_stocks.py (17 -- parsing/validation/backward-compatibility/runtime-mode) and test_live_equities.py (23 -- NVDA/TSLA/AAPL individually and together, provider-failure isolation, the TSLA-corroboration-leak regression test, live-mode absence semantics, convergence entity isolation, personalization separation confirming a new user never inherits interest in a live-configured ticker, and data provenance). backend test count: 227 -> 267 (+40); logan_core unchanged (306, no logan_core files touched). Combined 533 -> 573. mypy/ruff/black clean (one real new mypy fix: Callable instead of bare callable as a type annotation). Mobile: 100/100 Jest, tsc --noEmit, eslint all clean with zero code changes. Deferred/flagged for the owner: STOCK_EARNINGS_MISS/IN_LINE substitution, price-move/grade-only live opportunities (no earnings trigger), MARKETS/OIL needing a product decision about what real instrument each tracks, and BTC/FED/NFL/MUSIC/POLY/AI_SECTOR each needing a genuine new external vendor decision per domain -- none selected, none added, per the explicit instruction not to choose providers for those domains autonomously. No merge to main; commit not pushed pending review. See docs/DECISIONS.md ADR-060 for the full decision record.*
*Sprint 3.6.8 Block 1 changes (2026-08-22): added the first LLM integration to this codebase -- an optional, config-gated grounded-composition layer over Block 4's deterministic Ask STRATUS engine, never a replacement for it. Confirmed by inspection before writing any code: no LLM call existed anywhere in backend/, logan_core/, or mobile/ -- adding one is a genuine new external dependency and a genuine new secret, both requiring explicit owner confirmation under CLAUDE.md's collaboration model, obtained before implementation (official anthropic Python SDK, model claude-sonnet-5 -- the owner's explicit choice over the higher-reasoning-tier default on cost/latency grounds for a short grounded-composition task; ANTHROPIC_API_KEY approved as a new local-dev secret, gitignored, not populated with a real value by this block). New: ask_llm_provider.py (AskLlmProvider Protocol, AskLlmAnswer, build_system_prompt() -- vendor-agnostic, renders only real OpportunityContext fields Block 4 already uses, explicitly instructs the model not to invent market facts/prices/confidence values or contradict the given classification, and restates the ADR-002/010 advice boundary independently of the deterministic path's own copy), ask_llm_fixture.py (FixtureAskLlmProvider test double -- configured with a canned answer or error, records every call, no real network call anywhere in this block's test suite), ask_llm_anthropic.py (AnthropicAskLlmProvider -- the only file in this codebase that imports the anthropic package or knows any Anthropic-specific type; every SDK exception is mapped to the single domain error AskLlmProviderError at the boundary). ask_engine.py gains GroundedAnswer and generate_grounded_answer(), which is solely responsible for fallback -- AskLlmProviderError is never caught inside a provider, so disabled/unavailable/network/timeout/rate-limit/refusal/empty-or-malformed-response all fall through to the exact same, unchanged answer_question() Block 4 already had; no LLM failure mode can break Ask STRATUS. config.py gains llm_ask_enabled() (STRATUS_LLM_ASK, default off, same rollout pattern as STRATUS_LIVE_NVDA_EARNINGS/STRATUS_PERSIST_MEMORY); get_ask_llm_provider() (ask_engine.py) is a lazy, thread-safe, memoized construction point that degrades to None rather than crashing on a missing key or construction failure. main.py's ask_logan() now calls generate_grounded_answer() instead of answer_question() directly -- no other route logic changed. Prompt-injection hygiene is structural, not just instructional: the user's question is sent as a wholly separate user message, never concatenated into the system prompt string -- verified directly against a captured provider call, not just against prompt text. ASK_FOLLOWUP recording is unchanged and decoupled from which path produced the answer -- verified an LLM-failure-then-fallback question records exactly one ask_followup event, not zero and not two. AskRequest/AskResponse (models.py) are unchanged -- used_llm/llm_model stay internal to GroundedAnswer, no mobile contract impact, no mobile UI change this block. New tests: test_ask_llm.py (38 -- fixture provider, system-prompt grounding and injection-resistance, generate_grounded_answer() orchestration/fallback, config gating, AnthropicAskLlmProvider error-mapping/refusal/empty-response/success against an injected fake client, full /v1/ask route integration for every failure mode, and ASK_FOLLOWUP idempotency under all of them). Every pre-existing Block 4 Ask STRATUS test passes unchanged. backend test count: 141 -> 179 (+38); logan_core unaffected (306, untouched by this block). mypy/ruff/black clean on every new/changed file (two pre-existing mypy findings in Block 4's own test_ask_engine.py/test_ask_route.py predate this block and are unrelated **dict[str, object] kwargs-unpacking noise, not introduced or worsened here). Deferred, flagged for the owner: backend/.env was not given a real ANTHROPIC_API_KEY value -- this block has no real key to insert; STRATUS_LLM_ASK defaults off, so the system is fully functional and behaves exactly as before this block until the owner both enables the flag and adds a real key locally. No merge to main. See docs/DECISIONS.md ADR-056 for the full decision record.*
*Sprint 3.6.9 Block 1 changes (2026-08-23): first block of the post-3.6.8 gap analysis's Sprint 3.6.9 ("Beta Foundation: Remote Operation + Sports/Odds Intelligence"), scoped to remote-operation only -- Sports/Odds intentionally not started. Owner decisions made before implementation: Fly.io as the hosting target; SQLite kept on a durable Fly Volume for this beta stage (no PostgreSQL migration); STRATUS_PERSIST_MEMORY enabled for the hosted configuration; mobile API configuration in-scope; no external account/payment/secret created without the owner doing so directly. New root-level Dockerfile/.dockerignore/fly.toml make the repository deployment-ready without deploying anything -- the Dockerfile's build context is the repo root (not backend/) because backend/app/*.py's existing sys.path bridge to logan_core (ADR-022) requires both directories present as siblings, preserved rather than refactored; .dockerignore excludes backend/.env (a real secret-leak risk into the image otherwise, since it holds the real FMP_API_KEY/ANTHROPIC_API_KEY) and local *.db files; fly.toml pins min_machines_running=1/auto_stop_machines=false because the existing in-process notification poller (_notification_poll_loop, Sprint 3.6.6F) only works while the server process stays running, which Fly's default scale-to-zero would silently break. config.py gains legacy_memory_db_path() (makes the historical memory_engine.py prototype's SQLite path deployment-configurable -- it was hardcoded to ephemeral container storage), notification_store_db_path(), cors_allowed_origins(), and startup_config_summary(). New backend/app/notification_store.py (NotificationStore) durably backs registered push tokens and dispatch/review dedup state, reusing STRATUS_PERSIST_MEMORY rather than a second flag; a real bug was found and fixed while wiring it in -- dispatch_eligible_notifications()'s empty-token short-circuit ran before the store's lazy hydration, which would have silently defeated the persistence on the very first poll cycle after a real restart (fixed by hydrating first). STRATUS Watch fatigue/cooldown state was evaluated and deliberately not persisted this block -- a materially different, larger-shaped piece of PrioritizationEngine-internal state than the three flat dedup sets that were persisted, and the risk it's meant to guard against (a redeploy occasionally making Watch momentarily more willing to re-surface something) is already bounded by the independent per-event dispatch dedup, which prevents an actual duplicate push regardless; documented as a known, bounded gap rather than silently left unaddressed. CORS's hardcoded allow_origins=["*"] replaced with the new cors_allowed_origins(): demo/development mode keeps the exact prior behavior; beta/production mode defaults to an empty allowlist unless STRATUS_CORS_ALLOWED_ORIGINS is explicitly set -- CORS is browser-only enforcement, so this can never affect the React Native app's own requests. Mobile: new EXPO_PUBLIC_APP_ENV (set per eas.json build profile: development/preview/production) and constants/config.ts's new resolveApiBaseUrl()/isLanOrLocalUrl() enforce the explicit owner invariant that a preview/production build must never silently use a hardcoded LAN address -- development keeps the existing zero-config LAN fallback unchanged; preview/production throw a specific, readable error at load time if EXPO_PUBLIC_API_BASE_URL is unset, LAN/loopback-shaped, or not HTTPS. eas.json's preview/production profiles do not yet set EXPO_PUBLIC_API_BASE_URL (no Fly URL exists yet) -- building either profile today correctly throws until the owner adds the real hosted URL, the intended behavior for this stage rather than a gap papered over with a placeholder. New tests: test_deployment_config.py (11 -- path resolution, CORS mode defaults/overrides, startup summary never leaking a secret value), test_notification_persistence.py (6 -- disabled-vs-enabled contrast, token/dedup survival across a simulated restart, and the correctness-critical no-duplicate-push-after-restart case), test_beta_hardening.py's restart-safety-matrix test updated to assert the new persisted-token behavior (was asserting the now-superseded opposite), mobile lib/__tests__/config.test.ts (25 -- LAN/local detection, and resolveApiBaseUrl's full accept/throw matrix across development/preview/production). backend test count: 267 -> 284 (+17); logan_core unchanged (306, untouched by this block). mobile Jest: 100 -> 125 (+25). mypy (run from the repo root, matching the existing baseline invocation)/ruff/black clean; tsc --noEmit/eslint clean. No Fly.io account, app, volume, or secret created -- see the Block 1 report for the exact remaining owner steps. No merge to main. See docs/DECISIONS.md ADR-061 for the full decision record.*
*Sprint 3.6.9 Remote STRATUS + Mobile Identity + Beta Security changes (2026-08-23/24): after Block 1's deployment, real Fly.io CLI work stood up `stratus-api.fly.dev` (app/volume/secrets created, deployed, live NVDA/AAPL data verified over HTTPS, push-token persistence proven across a real machine restart) and wired `mobile/eas.json`'s preview/production profiles to the hosted URL, producing a real, physically-verified EAS preview build the founder installed and confirmed working over cellular with the dev PC's backend stopped (ADR-061 record, extended). Measuring real FMP usage (per explicit instruction, not guessed) found the hosted poller alone was already producing real HTTP 429s -- ~10,080 calls/day against a 250/day free-tier limit; fixed with a shared, endpoint-appropriate-TTL provider cache (`logan_core/receptors/providers/fmp.py`'s `FmpResponseCache`) bringing usage to a calculated 132-192 calls/day, safely under the limit -- no plan upgrade needed (ADR-062). The owner then supplied a real `ANTHROPIC_API_KEY`; imported into Fly and `STRATUS_LLM_ASK=true` enabled on the hosted beta (ADR-063) -- full grounded-path verification is the one explicitly deferred item, blocked on FMP's daily quota resetting, not a defect. Finally, Persistent Mobile Identity + Beta Security Boundary: reconnaissance found the founder-fallback default was a real, live information-disclosure exposure on the now-hosted API (`LOCAL_FOUNDER_USER_ID` is the public string `"demo_user"` -- any caller could obtain the founder's real personalized data with no guessing, via either an absent header or an explicit spoof). Fixed with a mode-aware `resolve_user_id()` (demo mode unchanged; beta/production mode never resolves to the founder constant from a client-supplied header, proven via two real end-to-end route-level tests), a real persisted per-install mobile identity (`mobile/lib/identity.ts` + centralized propagation through `apiClient.ts`), and a minimal in-memory rate limiter closing a zero-throttling gap found in the same attack-surface review, prioritizing `/v1/ask` given it can now trigger real metered Anthropic calls (ADR-064). `backend`/`logan_core` combined test count progression across this session: 590 → 605 (FMP cache) → 627 (identity/security). mobile Jest: 125 → 127 (Remote STRATUS closeout) → 134 (identity). mypy/ruff/black and `tsc --noEmit`/`eslint` clean throughout. No merge to main. See docs/DECISIONS.md ADR-061 through ADR-064 for the full decision records.*
*Stock Opportunity Logic V2 changes (2026-08-24/25): a full code-path audit (not docs) found two precise root causes for the "same card looks the same forever" product problem -- WorldModel's dedup window is measured against each signal's own captured_at, which never advances for an earnings/grade signal once first observed (fixed forever at the real report/action date), so the dedup window can never expire; and PrioritizationEngine.prioritize()'s changed_since_view parameter, which its own cooldown logic depends on, was never actually computed by any real caller and silently used its hardcoded True default. Built OpportunityLifecycleTracker (new logan_core/opportunity_lifecycle/ package), an opt-in PipelineDependency mirroring StockConvergenceTracker's exact pattern: seven bounded lifecycle states (new/developing/high_attention/monitoring/cooling/stale/expired), signal-specific decay windows (earnings 72h/240h/720h, price moves 6h/24h/72h, analyst actions 48h/168h/360h, convergence 24h/72h/168h), and a LifecycleDelta that finally feeds changed_since_view real data -- proven through the completely unmodified downstream pipeline. Notification eligibility redesigned so opportunity-exists no longer equals send-notification: a repeated poll of unchanged live data is alert-eligible once, then correctly not again, while the card remains visible. Durable persistence (backend/app/lifecycle_store.py, mirroring notification_store.py) proven across a real simulated restart. LLM-assisted delta-aware interpretation reuses the existing Sprint 3.6.8 Block 1 Ask STRATUS provider infrastructure entirely -- five additive OpportunityContext fields, a conditional system-prompt section instructing delta-oriented answers and forbidding invented changes; the deterministic tracker remains the sole author of every lifecycle fact. FMP capability audit: everything V2 depends on is already served by the three endpoints in use, zero new provider calls, no new vendor needed. Six new additive FeedItem API fields (lifecycle_state, is_updated, meaningful_change_type, lifecycle_reason, last_meaningful_change_at, thesis_age_hours), None/False when lifecycle tracking isn't active (demo mode). Gated behind live_stock_tickers(), same as trigger_detector/convergence_tracker -- demo mode byte-for-byte unchanged. New tests: test_opportunity_lifecycle.py (20), test_pipeline_lifecycle.py (5), test_lifecycle_integration.py (7), test_ask_lifecycle_grounding.py (5). backend/logan_core test count: 629 -> 666 (+37); 2 pre-existing tests updated (exact-field-set allowlists, real additions not leaks). mypy/ruff/black clean. No merge to main. See docs/DECISIONS.md ADR-066 for the full decision record, including the complete FMP capability table and the reference-implementation pattern documented for future Sports/Odds/Prediction Market domains. Deployed to hosted stratus-api (Fly release v7) the same session -- confirmed live via a recovery-pass curl of /v1/opportunities the same evening; recorded as an addendum to ADR-066 since the session was cut off before it could document the deploy itself.*
*Stock Opportunity Logic V2.1 -- User Sync Gap changes (2026-08-24): V2 answered "has the opportunity meaningfully changed since STRATUS last evaluated it" -- an objective question, identical for every user. V2.1 answers the next one: "has it meaningfully changed since *this specific user* last knew about it." Added a monotonic `revision` counter to LifecycleSnapshot/LifecycleDelta, bumped by OpportunityLifecycleTracker.observe() only on a genuinely *objective* meaningful change (a new, narrower `is_global_meaningful` gate that deliberately excludes a personal-relevance-only change -- two users must see the identical revision number for the same real-world opportunity). A durable, append-only OpportunityRevision history (backend/app/revision_store.py) is written only when the revision actually advances. New compact per-(user_id, entity_id) UserOpportunityKnowledge (logan_core/opportunity_lifecycle/sync.py), durably backed by backend/app/user_knowledge_store.py, tracks three high-water marks -- last_seen_revision, last_notified_revision, last_opened_revision -- updated via a single UPSERT path, never one row per interaction. Exact semantics: last_notified_revision advances only on a real successful Expo dispatch (notifications.py); last_seen_revision advances only inside record_interaction() for any real interaction_type (impression included, reusing Sprint 3.6.7 Block 3's existing surfaced-vs-API-response distinction); last_opened_revision advances only on "view" (the existing card-disclosure signal); the GET /v1/opportunities path itself never advances any pointer, structurally enforcing "fetching a feed does not imply seen." New pure compute_user_sync_delta() produces one of four deterministic statuses -- UP_TO_DATE, NEW_TO_USER, UPDATED_SINCE_SEEN, NOTIFIED_BUT_UNSEEN -- surfaced as two new additive FeedItem fields (opportunity_revision, user_sync_status) and extended into Ask STRATUS's OpportunityContext/system prompt (a "Sync" section grounding "what changed since I last looked" in the real sync summary, never invented). New tests: test_user_sync.py (12, pure), test_user_sync_integration.py (8, real backend wiring), 4 sync-grounding tests appended to test_ask_lifecycle_grounding.py. backend/logan_core test count: 666 -> 690 (+24); 2 pre-existing exact-field-set tests extended, 1 pre-existing fake-item test fixture extended -- all real additions, not leaks or weakenings. mypy/ruff/black clean. No merge to main. See docs/DECISIONS.md ADR-067 for the full decision record.*
